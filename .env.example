# 刘全密钥样例（开发规范 R20：只留变量名，绝无真值）
# 用法：cp .env.example .env 后填入真值；.env 已被 .gitignore 拦在库外
# 引用方式：models.yaml 的 env: 前缀（详设-v0.1 §7.1），业务代码不直接读这些变量
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=
LIUQUAN_ENGINE_DB_URL=
