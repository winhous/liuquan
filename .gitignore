# 密钥与本地环境（开发规范 R20：.env 永不入 git）
.env

# Python 虚拟环境与缓存
.venv/
__pycache__/
.pytest_cache/

# 打包产物
dist/
*.egg-info/

# pgserver 本地测试数据目录（tests 用到嵌入式 PG 时）
.pgdata/
tests/.pgdata/

# v0.5 扒图外部依赖（不入库）：
# XHS-Downloader vendor（186MB，从广成 studio/vendor/XHS-Downloader 部署时复制，
# 见 docs/部署-v0.5-外部依赖.md）
vendor/
tools/mermaid-render/node_modules/
tools/mermaid-render/package.json
tools/mermaid-render/package-lock.json

# 环境工具下载目录（非项目产物）
dsh-readme-research/
cookies.txt

# Figma MCP 本地工具配置（非项目产物）
.figma-mcp/
