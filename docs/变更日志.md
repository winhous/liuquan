# 刘全变更日志

> 只追加，不删除历史。按日期倒序（最新在上）。
> 格式：**因为** {原因}，**所以** 把 {旧} 变成 {新}

---

## 2026-08-28（筛选「选择即生效」：下拉框/复选框 onchange 自动提交）

- **因为** 用户复核反馈「从下拉框选择『已作废』后仍展示全部任务」——排查实测：服务端筛选链路本身正常（curl/Playwright 均验证 status=void 只回已作废任务），但**筛选表单不自动提交**，用户选完状态/逾期后必须再点「筛选」按钮才生效（且若 modal backdrop 残留挡住按钮则点了也无效），体验上表现为「选了没反应」，**所以** `web/templates/tasks.html` 筛选表单改进：状态下拉框与「仅看逾期」复选框加 `onchange="this.form.submit()"`——选择即自动提交（GET /tasks?status=...&overdue=...），选完立即刷新列表，不再依赖手动点筛选按钮；「筛选」按钮保留（关键词输入仍需手动提交，避免逐字刷新）。配套测试 `tests/test_web_tm.py` 新增 `test_filter_autosubmit_onchange`（模板断言 onchange 在位 + 服务端 status=void 链路断言），31 条全过；Playwright 真浏览器验证：选「已作废」不点按钮 → URL 自动变 `?status=void` → 列表只剩 1 行已作废；勾「仅看逾期」同样自动提交。四绿全绿。

---


- **因为** 用户复核 v0.2 提出三条反馈：①派生的子任务展示层面与父任务关联性不强；②任务清单的筛选按钮不能用；③问工序链的作用，**所以** 处理如下：
  - **①派生关联增强（决策 17 展示层落地）**：`web/tm_store.py` 的 `list_tasks` 带出父任务标题 + 子任务数（新增 `_TaskWithRel` 包装类，子查询父标题映射 + 子任务分组计数）；`web/app.py` 的 `_task_view` 增 `parent_title`/`child_count`；tasks.html 任务行——子任务显示**可点击的「由 t-xxx「父标题」派生」链接**（点进父任务）、父任务显示**「N 个子任务」徽章**（父子双向可见）。
  - **②筛选按钮修复（真实 bug + 体验）**：实测定位根因——modal 打开时 Tabler backdrop 遮罩挡住筛选按钮，若 modal 关闭异常会**残留 backdrop 导致按钮一直点不到**（Playwright 复现：modal 开时点筛选超时、ESC 关后恢复可点）。修复：tasks.html 脚本加 `hidden.bs.modal` 全局兜底（modal 完全隐藏后强制移除 backdrop + 解锁 body）；另加**筛选生效可见性**——筛选器加图标、`type="submit"` 显式声明、「已筛选」状态徽章（显示当前条件）+「清除筛选」按钮。筛选功能本身实测全部正常（状态/逾期/关键词/组合均生效）。
  - **③工序链答疑**：向用户说明工序链 = 预定义工序序列（如 tm_demo_chain = demo_echo→demo_propose），是「AI 建议自动变任务」的编排单元，详见下方对话解释。
  - 测试：test_web_tm 30 条全过（list_tasks 返回包装类不影响既有断言——路由层解包）；四绿全绿。Playwright 验证：派生链接 3 + 子任务徽章 1 + modal backdrop 残留 0 + 筛选可点 + 已筛选提示/清除按钮在位。

---


- **因为** v0.2 开发/验收/审计全部完成、等待用户复核，为让复核有据可依（对照操作而非凭感觉），**所以** 新增 `docs/复核报告-v0.2.md`：①A12-A26 验收断言逐条对照表（断言 → 页面/操作对应）；②页面复核操作路径（登录→看列表→看提案依据→触发演示链→批准→完成必填回复→试人工通道/编辑/派生/重开/筛选，7 步）；③引擎三接口 curl 演示命令；④端到端实测记录（2026-08-28 集成验收跑通：POST→DONE→转交器落库→批准→完成）；⑤交付规模（12 提交/472 单测/37 断言全绿/新增模块清单）。复核人可在文档上直接填「通过 / 需修改」结论。另清理开发残留（嵌入式 PG 测试簇残留进程与目录），docs/README 文档索引登记复核报告。本轮只动文档，未动代码。**待用户复核通过后 git tag v0.2。**

---


- **因为** 开发流程规定「每版发版（git tag）前跑一次合规审计」（文档与代码一致性、变更日志完整性、红线抽查），**所以** 在等待用户复核 v0.2 期间执行审计：①**红线抽查**——.env 未被 git 跟踪、代码无真密钥（DEEPSEEK_API_KEY 只存 .env）、无 Etsy API 调用代码、无 NocoBase 写操作（engine/web/models 全扫，排除 web/static 第三方资源）；②**文档-代码一致性**——详设 §9 交付文件清单逐项落地（engine/server.py/engine/actions/tm_proposal.py/models/tm.py/web/tm_store.py/web/engineapi/client.py/migrations/business/scripts/deploy 全部在位）；③**变更日志完整性**——v0.2 16 条条目齐全（详设撰写/决策 16/17/测试工具/T1-T8/文档同步）。审计通过，待用户复核后 git tag v0.2。

---


- **因为** T1/T3/T5/T6/T7 开发中各记录了「详设原文与实现」的偏差（均已在变更日志留痕但详设文档未同步），按铁律 2「开发中发现详设有误，先改详设再改代码」与 R25 文档同步精神，**所以** 回写 `docs/详设-v0.2-TM.md` 五处：①§3.3 task_event CHECK 补 `derived` 九态（原 8 值缺 derived，事件写入点表/A24 需要，T1 记录）；②§3.2 防重表述「唯一索引 + 查询双保险」改为「查询层双保险」（evidence 为 JSONB 数组无法直接建唯一索引，T1/T3 记录）；③§4.2 current_step 示例 2→1（0 起工序序号，2 步链 DONE 后为 1）+ audits 摘要字段补 result/tokens_out/duration_ms/created_at（实现超集，T5 记录）；④§3.3 done_at 语义补「voided 也写」（结束时间含作废，T6 记录）；⑤§9 文件清单 test_engineapi.py → test_engineapi_client.py（T2/T7 记录）。另 docs/README.md 详设行「待用户确认」→「定稿确认，v0.2 开发已完成 T1-T8」。本轮只动文档，未动代码。

---


- **因为** 用户复核前做最终文档核对，发现 `docs/版本规划.md` 总览表 v0.2 行仍写「TM 详设待撰写，开发未启动」（v0.1 行也还是「待 tag」而非已发版）——该文档在 v0.2 开发完成后未同步，**所以** 更新：总览表 v0.1 标「✅ 已发版（git tag v0.1）」、v0.2 标「开发完成 T1-T8 + 集成验收通过，待用户复核后 git tag v0.2」；v0.2 章节范围补决策 16/17、验收清单逐项打勾（[x] 已达成，对应 A12-A26 断言）。另校正 `docs/项目状态.md` 决策区标题「10 项」→「17 项」（决策 1-17 实际清单）。本轮只动文档，未动代码。

---


- **因为** T1-T7 全部完成（四绿全绿 472 单测、A12-A26 验收断言全绿），需做端到端集成验收收口，**所以**：①起引擎 server（8100）+ web（8090）真跑端到端——curl 三接口（POST 触发 tm_demo_chain 201 / GET 查 done 200 / registry 200 / 404 未登记链 / 422 坏入参），演示链真调 DeepSeek（353 tokens in/45 out/738ms）→ 转交器落库 tm.task_proposal（pending/suggest/ref_id=msg-88231/来源 e-000005）；②Playwright DOM 验收页面（未登录重定向登录、登录后任务中心标题、提案侧栏 2 条 pending、依据原文摘录可见、来源追溯可见、导航 12 项、逾期文案、零 404）；③Playwright 交互验收——批准提案 → 任务落库（open/source_type=ai/domain=demo/2 事件=created+approved 同事务，A13）；④A23 双保险实测——不填 result_note 直接 UPDATE done 被 chk_result_note 拒绝，填后成功（done+done_at）。
- **因为** web 页面 /tasks 渲染 500，日志定位 httpx.AsyncClient 构造失败 `Unknown scheme for proxy URL socks://127.0.0.1:7897`——httpx 默认 trust_env=True 读系统代理环境变量（本机 ALL_PROXY=socks://，与 v0.1 pydantic-ai 代理坑同款；web 侧 engineapi 客户端只连本机引擎服务不需要走系统代理），**所以** `web/engineapi/client.py` 的 AsyncClient 显式 `trust_env=False`（禁读系统代理），修复后页面 200。该坑此前单测未暴露（MockTransport 不触网络层），集成验收才抓到——印证集成验收价值。
- **因为** 生产部署需要常驻双服务（引擎 + web），**所以** 新增 `scripts/deploy/liuquan-engine.service` + `liuquan-web.service`（systemd 单元：Restart=always 崩溃自愈 A17、专用运行用户 liuquan 非 root、NoNewPrivileges、密钥只走 .env R20；部署路径 /opt/liuquan 待阿里云落地时按实际调整）。
- **因为** 集成验收确认 T1-T7 产物端到端可用（双服务 + 三接口 + 转交器落库 + 页面交互全通），**所以** 记 v0.2 集成验收通过（A12-A26 断言全绿 + 真跑画面），待用户复核后 git tag v0.2。

---


- **因为** 任务书要求 v0.2 交付「测试全套 + 验收断言 A12-A26」（详设-v0.2 §7.5 假数据工具 + §8 验收断言逐条可执行，全部可复现、零网络零真 token），**所以** 完成 v0.2 T7：
  - **假数据工具落地（详设 §7.5）**：新增 `tests/fixtures/tm/`（假 TaskProposal / 假任务 / 假 evidence 样本，结构对齐 models/contract/task.py 与 models/tm.py；`load.py` 加载即类型校验——pydantic model_validate / ORM 列集合+必填检查，结构漂移测试自然红）；新增 `tests/fake_providers.yaml` + `tests/fake_providers.py`（demo 域 Context provider 注册按环境加载，详设 v0.1 §13 模式：`demo.greeting` / `demo.inbox` 两个 Fake 实现只住 tests/，P3-4 执法生产目录出现 Fake 前缀 = 拒载；**FakeDemoInbox 返回「链喂给 AI 的数据集合」同时充当决策 16 禁幻觉白名单来源**——whitelist() = 数据集合对象 id，测试注入转交器做数据引用封闭性校验）。
  - **tests/test_antihallucination.py**（详设 §9 文件清单）：fixtures 样本 A/B/C/D 驱动禁幻觉三件套反向（A 合法落库 / B 集合外 ref_id 拒落记「幻觉证据: ref_id=...」/ C 空 evidence 拒落 / D audit_ids 不可查拒落）+ 样本加载即类型校验 + fake_providers.yaml 注册与白名单来源断言。
  - **tests/test_acceptance.py 追加 A12-A26**（@version_acceptance 标记沿用，每条至少一个测试函数、命名含断言号）：A12 三接口 POST 触发 tm_demo_chain -> 消费循环 DONE -> 提案落 pending（evidence 非空/ref_id 命中白名单/审计可查）；A13 批准同事务建 ai 任务+created/approved 双事件+回填、驳回不建任务、无自动通过路径（DB 不变量 pending 不带 task_id + 静态断言 web/tm_store.py 中 source_type="ai" 唯一出现处=approve_proposal）；A14 状态回流 open->in_progress->done 流水齐全 + done_at + 追溯闭环 task->提案->engine_task_id->引擎审计；A15 三接口 201/200/200 + 422/404；A16 三角色 cookie 中文标签 + 未登录重定向 /login；A17 崩溃自愈（recover 无检查点标 failed / 有检查点 resume 续跑不重烧 / 新 app 消费 queued 任务不丢）；A18 幂等同交付只落一条；A19 四绿标注（check.sh 四门 + 红即非零退出 + A12-A26 落点自检）；A20 双通道 + 来源徽章显示 domain；A21 禁幻觉三件套反向（转交器层样本 B/C/D + server 层白名单外 ref_id 链标 FAILED error 记幻觉证据）；A22 审核面板完整依据展示 + 无依据提案页面不可见；A23 必填回复 DAO 拦截 + DB CHECK 双保险、填后 done/void 落库且 note 进事件；A24 派生不结束 + derived 事件 + A 结束 B 不受影响；A25 防重 skip + 未完成任务挂起（自动清理机制不存在静态断言）+ 逾期提醒条常驻 + 筛选；A26 编辑 updated 事件 from/to 快照 + done/void 重开回 open。
  - **小改生产代码（A22 防御性）**：`web/tm_store.py::list_pending_proposals` 增 `jsonb_array_length(evidence) > 0` 过滤——转交器落库已保证 evidence 非空（禁幻觉三件套 ①），页面层再防御性过滤「无依据提案不可见」（详设 §6.2 决策 16 依据强制展示），不改审核流。
  - 复用 test_engine_server（_client/_post_demo/_wait_status/_wait_tm_count/_runner_kwargs/api_app）与 test_web_tm（_EngineStub/_login/_seed_task/_seed_proposal/_events_for/...）的跨模块 helper（fixture 随模块收集，test_acceptance 既有模式）。
  - 单测计数：存量 450（基线实测，任务书 449 为撰写时点）-> 新增 22 条（fixtures 驱动 6 + A12-A26 共 16）-> 全量 472 通过，四绿全绿（A12-A26 全进 `uv run pytest -m version_acceptance`，26 条全绿）。
  - **R25 交叉 review（T7 记录，无发现需返工）**：①详设 §8 断言逐条有落点（A12-A26 各至少一个测试函数，命名含断言号，A19 内置落点自检）；②详设 §9 文件清单对照——`tests/test_engineapi.py` 清单名与 T2 实际交付 `tests/test_engineapi_client.py` 不一致（T2 既有命名偏差，功能已覆盖，记录不改名）；`scripts/deploy/` systemd 单元未交付（属 T8 集成验收范围，非测试任务）；③§7.5 fake_providers 落地为测试侧注册（fake_providers.yaml + tests/fake_providers.py），未改生产 registry 的 context/demo.yaml（仅 demo_greeting 声明，与 test_workers 断言一致）；④状态文档同步：项目状态/变更日志/pyproject 的 version_acceptance 标记描述已同步 A12-A26。

## 2026-08-28（v0.2 T5：引擎常驻 server）

- **因为** 详设-v0.2 §2.3 常驻运行形态（单进程 = 三接口 + 队列消费循环）与 §4 三接口规格是 web 经 HTTP 碰引擎的唯一通道，v0.1 只有 CLI 同步执行（`liuquan-engine run` 一次进程跑完），**所以** 交付 `engine/server.py`（v0.2 T5）：
  - **FastAPI app**（前缀 /api/engine，三接口，与 CLI 同源复用 `engine/core/db.py` 的建任务 DAO）：`POST /api/engine/tasks` 建 engine_task 入队（trigger_type=manual）立即 201（链未登记 404 / input 不过链 input Model 422）；`GET /api/engine/tasks/{id}` 查状态/结果 200（status/current_step/error/output/finished_at/audits 摘要列表，§4.2；审计摘要用安全字段，§8 脱敏）；`GET /api/engine/registry` 工序/链/Action/事件清单 200（§4.3 结构）。
  - **后台队列消费循环**（`QueueConsumer`，§2.3）：concurrency 个 worker 协程并发消费（并发上限 2，asyncio.Semaphore），队列空退避 poll_interval；**TM 转交器钩子**（§5）：任务 DONE 后从链末步 output 提取 TaskProposal（六判据键 title/detail/domain/action_id/evidence/source，缺键/类型错不转交半成品），按 action_id 查 `engine.actions.CONSUMERS` 调消费者，注入 registry / whitelist / audit_lookup / tm_engine——rejected -> 任务标 failed（error 记拒落原因，§3.2 拒落 + 链 FAILED）；skipped_* -> 任务保持 done（决策 17 防重/幂等不改变链终态）；消费者抛异常 -> 标 failed（宁失败不假成功）。
  - **崩溃恢复**（§2.3 + 变更日志 M4 配套）：lifespan 启动先 recover() 处理遗留 running 任务——有检查点经 runner.resume 续跑（A4 不重烧 token）、无检查点标记 failed（v0.1 孤儿清理同哲学）；随后启动消费循环继续消费剩余 queued。
  - **白名单与审计 lookup 技术定**（任务书）：whitelist（决策 16 数据引用封闭性）v0.2 简化 = 链 input dict 递归收集 ref_id/id 字段字符串值（demo 链 input 的 ref_id 即白名单；真实业务 v0.3 由 Context provider 声明）；audit_lookup = 按提案 source.audit_ids 预取 engine_audit 可查 id 集合返回同步闭包（转交器契约 lookup 为同步可调用，validate_audit_ids 直接调用）。
  - **入口**：`__main__` uvicorn 启动（host 缺省回环、port 从 .env 的 LIUQUAN_ENGINE_API_URL 解析或缺省 8100，与 web 侧客户端缺省同源；P2：值只存 .env，代码零 URL/IP 字面量）。
  - **引擎库 DAO 扩展**（`engine/core/db.py`，只增不改）：`claim_task`（原子认领——SELECT FOR UPDATE SKIP LOCKED + UPDATE running 同事务，变更日志 M4 的 v0.2 落地，双消费者不重取同一任务）、`get_last_step`（链末步定位，链产物提取用）、`list_tasks_by_status`（崩溃恢复扫遗留 running）。
  - **runner 扩展**（`engine/core/runner.py`，只增不改）：`TaskRunner.consume_once()`——与 run() 同源（队列取 + 逐工序状态机 + 终态落库）但不创建新任务，只消费队列已有任务（claim_task -> 逐工序 -> finalize）。
  - 注入式设计（规范 R12）：`create_app` 的 engine / registry / runner / consumers / tm_engine 全部可注入，测试用桩（真 TaskRunner + FakeAgent / 脚本桩 runner / 记录桩转交器），零网络零真 token。
  - 交付 `tests/test_engine_server.py` 19 条：三接口各路径（201/404/422/200 + registry 结构）、队列消费循环（桩任务 DONE 后转交器被调用、rejected 标 failed、skipped 保持 done、非提案链不转交）、端到端真转交器落 tm.task_proposal(pending)（A12 在 server 层）、崩溃恢复（无检查点标 failed / 有检查点 resume 续跑不重烧 token）、claim_task 原子认领、纯函数（白名单递归收集/提案判据键/端口解析）。
  - **详设冲突记录（不改详设）**：详设 §4.2 示例 `"current_step": 2` 与引擎实际语义（current_step = 当前工序序号 0 起，DONE 后 = 末步序号 1）不一致——按引擎表语义实现（与 v0.1 engine_task.current_step 定义同源），示例仅为示意；详设 §4.2 的 audits 摘要示例只列 worker_id/model/tokens_in，实现补齐 result/tokens_out/duration_ms/created_at（字段集是 §4.2 示例的超集，web 客户端按 dict 透传不受影响）。

---

---

---

---

---

## 2026-08-28（v0.2 T6：web 接真数据——业务库 tm 表 + 引擎三接口）

- **因为** 任务书要求把页面原型 mock（web/app.py 内存 TASKS/PROPOSALS）替换为真实数据库 + 引擎调用（详设-v0.2 §6），且 P3-2 执法「web/ 不得 import engine.*」（双向零代码耦合 R24），**所以** 新增 `web/tm_store.py`（web 侧独立 DAO：只依赖 models/tm.py 共享 ORM + SQLAlchemy async，连接串经 dotenv_values 读 .env 的 LIUQUAN_TM_DB_URL，独立实现 create_tm_engine（先例 engine/actions/tm_proposal.py 模式但不 import 它）；方法：list_tasks（状态/逾期/关键词筛选）/ count_overdue / stats / list_pending_proposals / create_task（manual，source={"creator": 操作者}）/ transition（状态机代码写死 §3.5.1：started/completed/voided/blocked/unblocked，每次写 task_event，done/void 写 done_at，completed/voided 事件 note=result_note，blocked 必填 等物料/等回复）/ derive_task（derived_from=原任务，原任务不结束，新任务 created 事件 + 原任务 derived 事件 note=派生任务 id）/ edit_task（updated 事件 from/to 快照 JSON）/ reopen_task（done/void->open，清 result_note/done_at + updated 事件）/ approve_proposal（单事务：提案 approved + reviewed_by/reviewed_at + 建 task（source_type=ai，source 复制提案来源并追加 proposal_id）+ 提案 task_id 回填 + created/approved 双事件）/ reject_proposal（rejected + reject_reason 不建任务）；业务拒绝抛 TMWebError（路由捕获 -> 页面 err 提示），DB CHECK（chk_result_note/chk_blocked_reason）兜底双保险）。
- **因为** 路由需要 async DB 调用、且测试要注入嵌入式 PG store 与引擎桩（R12 构造注入），**所以** 重构 `web/app.py`：create_app(tm_store=..., engine_client_factory=...) 工厂 + 模块级 app=create_app()（uvicorn 不变）；/tasks 改 async（状态/逾期/关键词筛选 + 逾期提醒条计数 + 提案侧栏 + 触发面板 registry 链清单，引擎未连接时降级展示不 500）；新增 POST /tasks/status|create|derive|edit|reopen、POST /tasks/proposals/approve|reject、POST /tasks/trigger（选链填参 -> engineapi create_task -> 重定向带 engine_task_id）、GET /tasks/engine/{id}（轮询代理 -> JSON terminal 标记，JS 查终态后刷新提案栏）；登录/导航/占位页保留，未登录访问 /tasks 重定向 /login（验收 A16）；完成/作废弹窗 result_note 必填（HTML5 required + 服务端校验 + DB CHECK 双保险，A23）。
- **因为** 页面要接真数据并展示全部 v0.2 要素（详设 §6.2），**所以** 重构 `web/templates/tasks.html`：逾期提醒条常驻（数量 + 查看逾期任务入口）/ 4 统计卡真实聚合 / 任务表（列 任务/状态/负责人/截止/来源 + 操作；来源徽章=domain；派生提示「由 t-xxx 派生」；逾期标红；无优先级列）/ 筛选器（状态/仅看逾期/关键词）/ 状态操作按钮（开始/完成/作废/阻塞/解除阻塞/重开 + 编辑/派生）+ 完成作废必填回复弹窗 + 阻塞原因弹窗 + 编辑/派生/人工建任务/驳回弹窗 / 提案侧栏（风险徽章 + 依据区每条 evidence 类型+ref_id+原文摘录 + 查看入口预留 + 来源 chain_id+engine_task_id+审计条数 + 批准/驳回）/ 触发演示链面板（registry 链清单 + 按链声明表单 + 轮询 JS）。
- **因为** 任务书验收要求 TestClient + tm_pg_cluster 嵌入式 PG 覆盖各路径（任务列表/筛选/状态操作/派生/编辑/重开/人工建任务/提案审核/逾期提醒条），**所以** 新增 `tests/test_web_tm.py` 29 条：登录与角色（A16：未登录重定向 /login、cookie 映射中文标签）/ 任务列表与逾期提醒条计数（done/void 不计数）/ 三筛选 / started / completed 与 voided「必填 result_note 拒后成功」（A23）/ blocked（必填原因+非法原因拒+unblocked 清空）/ 非法迁移拒 / 派生（原任务不结束 + derived 事件，A24）/ 编辑（updated 快照，A26）/ 重开（done->open 清 result_note/done_at）/ 人工建任务（source_type=manual + source.creator + 来源域，A20）/ 提案批准（事务内 approved+建任务+双事件+task_id 回填+source.proposal_id，A13；重复批准拒）/ 驳回（rejected+reason 不建任务）/ 提案侧栏依据展示（A22）/ 触发面板（registry 链清单 + POST 序列化断言 + 轮询 queued->done terminal 翻转 + 缺参拒 + 引擎 404 降级 + /tasks 引擎挂仍 200）。引擎三接口用 _EngineStub + httpx.MockTransport 构造注入（R12：零网络零真服务，桩只住 tests/）；tm_engine 用 NullPool（TestClient portal 循环与 pytest 循环不串连接）。
- **因为** 详设 §6.2 的「筛选条件明细后定（⚑）」本任务按任务书先做状态+逾期+关键词（决策 17 第 5 条），**所以** 列表筛选器实现这三个；负责人/来源域筛选留待后续（查询层已按 role/domain 预留过滤参数位，权限留位 §3.5.6 不阻塞）。

---

## 2026-08-28（v0.2 T3：TM 转交器 Action 消费者）

- **因为** 详设-v0.2 §5 要求「引擎不直接写业务库——链完成后由注册的 Action 消费者（纯代码）把 TaskProposal 落 tm.task_proposal」（AI 100% 可控，AI 全程不碰库），**所以** 新增 `engine/actions/` 包：`tm_proposal.py`（TM 转交器 `consume_task_proposal`：契约类型校验（R2，dict 形态归一）→ 禁幻觉三件套（①evidence 非空，复用 assert_suggest_has_evidence 语义；②audit_ids 非空时经注入的 audit_lookup 查引擎库可查（validate_audit_ids 模式），空 = 纯代码工序产出按工序声明 reason:none 豁免（任务书技术决策：无 LLM 调用本身可追溯——engine_task_id 可查引擎任务与步骤），reason=llm 却空 audit_ids 拒落；③数据引用封闭性：evidence 每个 ref_id 必须命中注入的 whitelist，白名单外拒落 + 报告「幻觉证据: ref_id=...」）→ risk 代码规则标注（查 registry Action 声明，缺声明拒落，R9）→ 幂等（source.engine_task_id + worker_id 已存在跳过）→ 同事件防重（同 ref_id + action_id 已有待审提案/未完成任务跳过，决策 17）→ 落 tm.task_proposal(status=pending)，evidence/source 按契约 JSONB 映射）+ `__init__.py`（消费者注册表 CONSUMERS，v0.2 仅 tm.proposal 一条）。注入式设计（R12）：tm_engine（缺省经 create_tm_engine 从 .env 的 LIUQUAN_TM_DB_URL 建，dotenv_values 读 .env 不碰 os.environ，P2 先例 engine/core/db.py）/ registry（缺省惰性 load_registry）/ audit_lookup / whitelist 全部可注入，缺注入 fail-closed 拒落（宁失败不假成功）；不 import web 任何代码（P3-2/R24 双向零代码耦合）。
- **因为** 转交器是 v0.2 禁幻觉与防重的机器校验落点（验收 A18/A21/A25），**所以** 新增 `tests/test_tm_proposal.py` 21 条：消费者注册表映射 / 正常落库（status=pending、risk 由 Action 声明标注 suggest、evidence/source JSONB 契约对齐、dict 形态输入）/ 三件套反向各一（空 evidence、audit_ids 不可查、白名单外 ref_id 记「幻觉证据: ref_id=...」）+ 多证据部分越界 / 纯代码工序 audit_ids 空豁免与 reason=llm 拒落 / 缺注入 fail-closed（whitelist/audit_lookup 未注入、action 未登记）/ 幂等（同 eid+wid 重复交付只落一条）/ 防重（待审提案、未完成任务跳过；任务 done 放行；不同 ref_id 放行）/ 端到端（真声明 tm_demo_chain + FakeAgent 桩零网络 DONE 后转交器落库 + 重跑幂等）。
- **因为** 详设 §3.2「防重键唯一索引」与现有迁移 0001 不一致（迁移无 tm.task_proposal 唯一索引；ref_id 在 evidence JSONB 数组内，表达式唯一索引需新迁移），**所以** 本任务先落「查询层双保险」（待审提案查询 + 未完成任务查询，转交器代码内），唯一索引留待后续迁移任务补充（设计冲突已记录，未擅改迁移与详设）。

---

## 2026-08-28（v0.2 T4：demo_propose 工序 + tm_demo_chain 演示链 + registry 登记）

- **因为** 详设-v0.2 §7 演示链需要「纯代码工序（无 LLM 调用）」把 demo_echo 结果组织成 TaskProposal 提案（演示「AI 建议 -> 提案 -> 任务」闭环而不增加 token 成本），**所以** 新增 `engine/registry/workers/demo/demo_propose/` 工序（worker.yaml：domain=demo / risk=suggest / 输入 DemoProposeInput / 输出 TaskProposal 契约 / reason: none / config/ 外置规格；schema.py 契约位；run.py 纯代码把输入组织成 TaskProposal：title/detail 用输入内容经 config 模板生成、action_id=tm.proposal、默认角色按 config 声明、evidence 引用链输入 ref_id（决策 16 数据引用封闭性）、无依据输入（空 ref_id/文本）ValueError 拒产（详设 v0.1 §6.4 无依据不出建议）；prompt.md 说明无 LLM 调用）。
- **因为** 纯代码工序要让 runner 跳过 REASON（不调 LLM、零 token、零审计），**所以** 工序声明 schema（WorkerDeclaration）增 `reason` 字段（llm=调 LLM 缺省 / none=跳过 REASON，INIT 直通 ACT），runner REASON 相位对 reason:none 工序直通（技术定，详设-v0.2 §7 已补记）。
- **因为** demo_propose 以 suggest 风险产出提案（§7），超出 v0.1 §5.2 demo 域风险上限 read（INIT Policy 会拒），**所以** demo 域风险上限 read → suggest（policy.py + 详设 v0.1 §5.2 表同步；demo 为演示域无真实业务写，不引入风险）。
- **因为** TaskProposal.source 需填 chain_id（§3.4 来源追溯结构），而 EngineContext 此前不带链上下文，**所以** EngineContext 增 `chain_id` 字段（缺省 None），runner 在 ACT 装配时透传（run/resume 两条路径，技术定）。
- **因为** 演示链要把 echo 结果与证据引用串起来，**所以** 新增 `engine/registry/chains/demo/tm_demo_chain/chain.yaml`（demo_echo -> demo_propose，链入参 = DemoProposeInput：text/ref_id/kind；step2 显式映射 steps[0].output.text + task.input.ref_id/kind）；`engine/registry/actions/demo.yaml` 增 `tm.proposal` Action 登记（target=tm.proposal, risk=suggest, output_model=TaskProposal）；L9 登记文件重生成（新增 DemoProposeInput/TaskProposal 两条记录，存量 hash 不变）。
- **因为** 新工序改变了注册表清单，**所以** 同步测试：新增 `tests/test_demo_propose.py` 7 条（模型往返/合法 TaskProposal 断言/evidence 引用链输入/无依据输入被拒/真链 tm_demo_chain 全链路 DONE 且全链只调 1 次 LLM）；更新 test_workers/test_cli（注册表集合与清单计数）+ test_policy/test_runner（demo 域风险上限 suggest 后的断言与文案）。

---

## 2026-08-28（v0.2 T1：业务库 Alembic 迁移 + 业务表 ORM）

- **因为** v0.2 任务 1 = 详设-v0.2-TM §2.2（迁移两套：新增 `migrations/business/`）+ §3（tm schema 三表 DDL）+ §5（业务表 ORM 住 `models/tm.py`，web 与引擎转交器共用，R22 唯一通用语言），**所以** 新增：①`models/tm.py`（`TmBase` + `Task`/`TaskProposal`/`TaskEvent` 三表 ORM，纯数据层：字段/类型/默认值/CHECK/索引/FK 与迁移 DDL 逐列同源）；②`migrations/business/`（alembic.ini + env.py 读 `.env` 的 `LIUQUAN_TM_DB_URL`（dotenv_values，P2 规则 4 合法来源，不触碰 os.environ）+ versions/0001 手写 DDL：三表 + 具名 CHECK `chk_blocked_reason`/`chk_result_note` + 5 条内联 CHECK + 4 索引（`idx_task_status_due`/`idx_task_role`/`idx_proposal_status`(status, created_at DESC)/`idx_event_task`）+ 业务库内 FK 三处（task.derived_from 自引用 / proposal.task_id / event.task_id ON DELETE CASCADE），跨库不建 FK（R23））；③`tests/conftest.py` 增 `tm_pg_cluster` 嵌入式业务库 fixture（复用 engine 簇另建 liuquan 库 + tm schema + alembic upgrade head 真跑，经 `-x db_url=` 传连接串，不注入环境变量）；④`tests/test_tm_models.py` 18 条（schema 同源：列集合/JSONB/DATE/CHECK 数量/索引/外键；冒烟：三表插入读取、derived_from 派生、event 级联；决策 17 反向：done/void 无 result_note 被拒、blocked 无 blocked_reason 被拒、非法状态/超长标题/非法事件类型被拒）。真库 `alembic upgrade head` 已跑通（`\dt tm.*` 三表、`\d tm.task` 7 条 CHECK + 3 索引 + 自引用 FK 齐全），downgrade base 后重跑 upgrade 亦通。**已知详设冲突（未擅改详设，记此供核对）**：①§3.3 task_event DDL 的 event_type CHECK 原文缺 `derived`（§3.3 事件写入点表与验收 A24 均需要 derived 事件），实现按任务要求与事件写入点采用九态（含 derived）；②§3.2 文本「防重键用 tm.task_proposal 的唯一索引 + tm.task 查询双保险」但 DDL 未含该唯一索引（按 DDL 实现，防重走转交器应用层查询，唯一索引形态留待后续任务评估——evidence 为 JSONB 数组，直接唯一索引不可表达）。**R25 review**：状态类文档无过期（项目状态 v0.2 开发中不变）；详设 §2.2/§3/§9 文件清单 `migrations/business/` 与 `models/tm.py` 均已落地，无偏差；并行子代理 T2 的 `tests/test_engineapi_client.py:310` 文档字符串含 IPv4 字面量（P2 零容忍）导致共享四绿门红，已最小修复（改为「回环地址」，仅动文档字符串，不改行为）并如实上报。

---

## 2026-08-28（v0.2 T2：web 侧引擎三接口 HTTP 客户端）

- **因为** v0.2 任务 2 = 详设-v0.2-TM §4.4 的 web 侧客户端（web 只经 HTTP 三接口碰引擎，双向零代码耦合，P3-2 执法），**所以** 新增 `web/engineapi/` 包：`client.py`（httpx.AsyncClient 封装 `create_task(chain_id, input, trigger_ref)` / `get_task(task_id)` / `list_registry()` 三方法，对应详设 §4.1/4.2/4.3，期望 201/200/200；404→`EngineNotFoundError`、422→`EngineValidationError`（均带响应体 detail）、其余非预期状态→`EngineStatusError`，网络层异常（连接/读超时等）统一包成 `EngineConnectionError`（cause 保留原始 httpx 异常），基类 `EngineAPIError` 携带 status_code/detail）+ 空包标记 `__init__.py`；base URL 经 `dotenv_values` 读仓库根 `.env` 的 `LIQUAN_ENGINE_API_URL`（P2 规则 4 合法来源，不触碰 os.environ，先例 engine/core/db.py），未配置/为空回退本地缺省地址（按段拼接构造 `_DEFAULT_BASE_URL`——P2 零容忍下 URL/IP 不得成完整字面量，与 engine/lint/p2.py 自检同款）；默认超时连接 5s/读 30s（`DEFAULT_TIMEOUT`，httpx 0.28 四参数全给）；**依赖**：`pyproject.toml` 把 httpx 提升为直接依赖（此前为 pydantic-ai 依赖树传递引入，uv.lock 仅 +2 行版本不变）；**测试**：`tests/test_engineapi_client.py` 16 条（httpx.MockTransport 构造注入 client 的 transport 参数，零网络零真服务，覆盖三方法请求路径/方法/参数序列化/响应解析/404 与 422 异常路径/base URL 解析与缺省回退/网络异常包装/默认超时）。**未动** `web/app.py` 原型逻辑（web 接真数据是后续任务 6）。**R25 review**：状态类文档无过期（项目状态「v0.2 开发中」不变，任务进度由各 commit 记录）；详设 §4.4 文件清单两项与 §9 文件清单 `web/engineapi/` 均已落地，无偏差。

---

## 2026-08-28（v0.1 发版 tag + v0.2 开发启动）

- **因为** 用户复核通过 v0.1（四绿全真跑 + A1-A11 全绿 + 真模型冒烟），并确认 TM 详设（含决策 16/17 细则），**所以** ①`git tag v0.1`（v0.1 引擎地基正式发版）；②启动 v0.2 开发：本地建业务库 `liuquan` + `tm` schema（pgdev.sh），`.env`/`.env.example` 增 `LIUQUAN_TM_DB_URL`（业务库连接）与 `LIUQUAN_ENGINE_API_URL`（引擎常驻服务地址，web 经此调三接口）；③v0.2 任务按详设 §9 文件清单拆解为 8 个子代理任务分批执行（业务库迁移+ORM / engineapi 客户端 / TM 转交器 / demo_propose 工序 / 引擎 server 三接口 / web 接真数据 / 测试 / 集成验收），主代理逐项四绿验收。

---


- **因为** 用户补充任务生命周期的 5 条关键规则（并要求参考网上成熟任务系统设计；调研了 [Jira transition 必带 resolution](https://ja.confluence.atlassian.com/jirasoftware/transitioning-an-issue-1688898767.html)、[Jira 子任务不自动关父任务](https://docs.apps.decadis.net/jira-workflow-toolbox-data-center/all-sub-tasks-must-be-done-or-closed.md)、[Teambition 任务关联](https://www.php.cn/faq/2375351.html#1)、[禅道任务流转](https://www.zentao.net/faq/global/c1188)、[工单状态机闭环](http://www.xnwp.cn/news/7976)）：①一个任务两种结束方式（结束 / 派生新任务），派生不结束原任务，**不管什么方式必须人工回复，不回复不能结束**；②权限现在不做、将来必须引入完善权限系统（精确到人和数据，同页面不同人看到不同任务数量与内容）；③同一件事不生成同样的任务，但任务不完成就一直挂着；④任务生成后有权限人员可编辑；⑤逾期一直提醒 + 列表必有筛选，**所以** 记录决策 17 并落入详设：
  - **§3.1 task 表**：新增 `result_note`（结束前必填人工回复，DB CHECK `chk_result_note` 强制 done/void 必须有值）+ `derived_from`（派生来源，派生≠原任务结束）。
  - **§3.2 提案防重**：同链重跑幂等之外，新增**同事件防重**——同业务对象（evidence ref_id）+ 同建议类型（action_id）已有未完成任务/待审提案 → 本次提案不落库（skip 日志）；未完成任务一直挂着不自动清理。
  - **§3.3 task_event**：completed/voided 的 note=result_note（执行结果回流的事实基础）；新增 derived 事件（派生记录，派生不结束原任务）。
  - **§3.5 新增任务生命周期专节**：状态机结束条件收紧（done/void 必填回复）；派生语义（参考 Jira 子任务不自动关父任务）；防重挂起；逾期常驻提醒条 + 列表筛选（筛选条件明细后定 ⚑）；编辑（updated 事件带 from/to 快照、done/void 可重开）；**权限留位**（v0.2 不做、v0.2 后必须引入精确到人和数据的行级权限，查询层预留 role/domain 过滤参数）。
  - **§6 页面要素**：逾期提醒条（常驻）、列表筛选器、完成/作废弹窗必填回复、派生新任务入口、编辑入口、重开按钮。
  - **§8 验收断言扩为 A12-A26**：A23 必填回复（页面拦截 + DB CHECK 双保险）、A24 派生不结束（A 派生 B 后 A 不自动 done）、A25 防重+挂起+逾期提醒条+筛选、A26 编辑留痕+重开。
  - **同步**：项目状态决策 17、建设方案 v0.2 第 3 项、详设 §1 范围、§9 文件清单（web 职责补派生/编辑/逾期提醒/筛选）。

---


- **因为** 用户提醒「之前定过一个专门出假数据的工具，可以给任务部分提供测试」——即 R12 桩规范 + 详设 v0.1 §13 的假数据工具三件套（FakeAgent 五件套 / tests/fixtures/ 样本 / fake_providers.yaml Context 通道桩，业务方 2026-08-27 确认的三通道假数据模式），TM 详设此前只列了测试文件没写清假数据来源，**所以** 详设 v0.2 新增 §7.5「测试假数据工具」：①桩家族五件套复用驱动 tm_demo_chain 各路径（零网络零 token）；②新增 `tests/fixtures/tm/` 假 TaskProposal/假任务/假 evidence 样本（结构对齐 models/tm.py 与契约，加载即类型校验）；③fake_providers.yaml 扩展 demo 域 provider，返回「链喂给 AI 的数据集合」——**同时充当决策 16 禁幻觉白名单的来源**：测试里 ref_id 命中集合 = 通过，构造集合外 ref_id = 拒落（A21 反向测试的样本 A/B/C/D 四态写死）；④假数据工具只住 tests/（lint P3 规则 4 延续），页面层测试用 TestClient + 嵌入式 PG。§9 文件清单同步。本轮只动文档，未动代码。

---


- **因为** 用户补充任务生成的关键业务规则：①任务生成必须包含 AI 生成 + 人工输入两种方式；②任务来源是 ERP/CRM/SEO 等业务系统 + 任务系统本身；③审核 AI 提案时，人必须能点开跳转到对应业务系统看上下文，或 AI 必须把生成任务的依据列清楚，**这里绝对 100% 禁止幻觉**，**所以** 记录决策 16 并全链路落入详设/建设方案：
  - **双通道**：AI 通道（引擎链产出 TaskProposal → 人工审核 → 批准变任务）+ 人工通道（任务中心「新建任务」直接落 tm.task，source_type=manual）；task 表原设计即含 ai/manual 两值，本次明确拍板并补人工通道页面入口。
  - **来源域**：task 表新增 `domain` 字段（crm/erp/seo/tm/...），AI 任务继承提案 domain、人工任务创建时选择；任务列表按域标来源徽章——"这任务谁提的"一眼可见。
  - **审核依据两条路都给**：审核面板强制展示每条 evidence（类型 + 业务对象 id + 原文摘录 quote），审核人据此判断 AI 不是瞎说；证据点「查看」跳对应业务系统上下文页（v0.2 业务系统未建：展示依据全文 + ref_id，跳转机制预留，CRM/ERP 落地时各业务域注册 evidence 查看 URL 解析器）；审计兜底：点来源 audit id → 引擎库查那次 LLM 调用输入输出。
  - **禁止幻觉三件套（机器强制，非 AI 自觉）**：①evidence 非空拒落（v0.1 §6.4 已有）；②audit_ids 每个可在引擎库查到对应调用（v0.1 已有）；③**数据引用封闭性（新增）**：evidence 的每个 ref_id 必须命中本链喂给 AI 的数据集合（链 Context provider 声明的对象 id 白名单）——AI 只能引用它真看过的数据，引用白名单外业务对象 = 转交器拒落 + 链 FAILED（error 记「幻觉证据: ref_id=...」）。
  - **验收断言扩为 A12-A22**：新增 A20 双通道生成（AI + 人工各自成立、来源徽章显示 domain）、A21 禁幻觉三件套反向测试（空证据/不可查 audit/白名单外 ref_id 各一条）、A22 审核面板依据完整展示（无依据提案页面不可见）；tests 清单加 test_antihallucination.py。
  - **同步**：建设方案 v0.2 第 3/4 项补决策 16 细则；项目状态决策 16；详设 v0.2 §1 范围、§3.1 task.domain、§3.2 审核流与禁幻觉三件套、§3.4 source 说明、§5 转交器校验、§6 页面要素、§7 演示链、§8 断言、§9 文件清单、§10 决策点。

---


- **因为** v0.1 已全部完成（A1-A11 全绿）、v0.2 页面原型完成（导航定案 + Tabler），按铁律 2「详设先行」下一大步是 v0.2 的 TM 详设（建设方案 v0.2 全部实现项 #1-#9 的依据），业务细则已拍板（决策 11 修订/12/15 + 导航定案），**所以** 撰写 `docs/详设-v0.2-TM.md`：①**双 database**（业务库 liuquan + tm schema，跨库不建外键；Alembic 两套：migrations/engine 已有不动 + migrations/business 新增）；②**tm 三表字段级**——task（title/detail/role/due/status 五态/blocked_reason 必填「等物料·等回复」/source_type/source JSONB/来源追溯，无优先级、逾期标红）、task_proposal（pending/approved/rejected 三态 + risk 代码规则标注 + evidence/source 对齐契约）、task_event（created/approved/started/completed/voided/blocked/unblocked/updated 八类流水）；③**提案审核流**（决策 12 全人工批/驳，批准=提案 approved + 建任务 + 事件同事务，驳回=rejected 不建任务；转交器幂等去重）；④**引擎三接口**（POST /api/engine/tasks、GET /api/engine/tasks/{id}、GET /api/engine/registry，web 只经 HTTP 碰引擎，双向零代码耦合，P3-2 延续）；⑤**常驻形态**（engine/server.py = 三接口 + 后台队列消费循环，并发上限 2，systemd 双服务）；⑥**TM 转交器**（engine/actions/tm_proposal.py：链完成回调代码落库，AI 不碰库；业务校验 evidence 非空 + audit_ids 可查；业务表 ORM 住 models/tm.py 供 web 与转交器共用）；⑦**回流语义**（v0.2 = task_event 流水 + 追溯闭环 task→提案→engine_task→审计；完整事件闭环 v0.4）；⑧**演示链** tm_demo_chain（demo_echo 已有 + demo_propose 纯代码工序产出 TaskProposal）；⑨**验收断言 A12-A19**（8 条：触发链→提案、全人工批驳、状态回流、三接口 curl、登录角色、崩溃自愈、幂等、四绿）；⑩**交付文件清单**（engine/server.py + engine/actions/ + demo_propose 工序 + models/tm.py + web/engineapi/ + migrations/business/ + systemd 单元 + 测试）。**状态：待用户过目确认后进 v0.2 开发（铁律 2：未确认不动代码）。** 同步：docs/README 文档索引补详设行、CLAUDE.md 文档列表与状态行、项目状态.md（当前阶段/版本进度/下一步）。

---

## 2026-08-28（状态/规划类文档同步，消除过期表述）

- **因为** 用户核对「已开发的部分，相关文档是否按要求更新」，逐份审查发现 4 份状态/规划类文档与项目实际有偏差（变更日志/详设 v0.1 §6.4/建设方案 v0.2 已同步，但版本总览、概设决策表、验收画面等仍停留在 v0.1 前或残留 P0-P3 优先级表述），**所以** 批量同步（本轮只动文档，未动代码）：
  - `docs/版本规划.md`：总览表 v0.1 标「✅ 全部完成（待 tag）」、v0.2 标「页面原型完成 / TM 详设待撰写，开发未启动」；v0.2 范围补「无优先级分级（决策 11 修订）+ 工作台占位（决策 15）」；v1.0 总验收第 1 条「按优先级排好的跨域任务清单」改为「跨域任务清单（标来源；逾期标红，无优先级分级——决策 11 修订）」；「需业务输入」表 TM 细则行标注 2026-08-28 修订。
  - `docs/概要设计-v0.3.md`：§4.1 任务一等公民字段删「优先级」（决策 11 修订取消分级）；§4.1 验收形态与 §6.2 总验收第 1 条去「按优先级排好」并注逾期标红；§7 决策表决策 11 行标注 2026-08-28 修订（取消 P0-P3 分级）、补决策 14/15 行（此前决策表只到 13）；决策 14 原文「+ 任务优先级」注随修订失效。
  - `docs/建设方案.md`：v1.0 总验收第 1 条「按优先级排好的跨域任务清单」同步为无分级表述（与版本规划/概设口径一致）。
  - `docs/项目状态.md`：更新日期与当前阶段补 v0.2 页面原型完成；路线图「页面原型 ✅（v0.2 框架）」；仓库现状树补 `web/` 目录（v0.2 原型，Tabler vendored 零 CDN）；版本进度表 v0.2 行「未启动」→「页面原型完成，TM 详设待撰写」；git 提交数 27→39；决策 14 注修订；下一步第 1 项改为 A11 冒烟已跑通（待用户复核 tag）、第 2 项明确写 `docs/详设-v0.2-TM.md`。
  - `CLAUDE.md` / `docs/README.md`：当前阶段行补 v0.2 原型完成与 TM 详设待撰写（含决策 11 修订/15 要点）。
- **因为** 决策 11 修订（取消 P0-P3）此前只同步了变更日志/契约/详设 §6.4/建设方案 v0.2/项目状态决策区，概设 §7 决策表与各处「按优先级排好」总验收画面漏改，**所以** 本次一并补齐，保证「无优先级分级」在全文档口径一致（后续 v0.2 详设与开发均以此为准）。

---


- **因为** 用户拍板导航结构（替代上一版的「工作台组+二级」方案）：工作台一级菜单当前版本**无内容**（统计等内容后续在定，决策 15 方向保留）；任务中心一级菜单**当前保持不变**（任务列表页 + 提案侧栏），二级菜单机制预留（MODULES children 结构已支持）但暂不填充；**CRM / ERP 增强展示二级菜单占位**（对话翻译/客户快照/跟进待办 · 库存/补货建议/异常预警，全部建设中）；SEO / 扒图保持一级占位，**所以** 原型调整：`web/app.py` MODULES 重构（工作台 id=workspace 独立占位、任务中心 id=tm 指向 /tasks、CRM/ERP 带 children 占位）、删除上一版新增的 /proposals 与 /stats 路由与模板（提案回到任务页侧栏、统计待工作台内容后续在定）、module.html 展示模块二级占位子项；决策 15 与建设方案 v0.2 第 9 项同步调整（统计方向确认、时机后移）。Playwright 验证：顶级菜单 工作台/任务中心/CRM/ERP 增强/SEO/扒图、CRM 与 ERP 各 3 个二级占位子项、任务中心 8 任务行、根路径→/tasks、零 404。

## 2026-08-28（决策 15：统计数据一定会做）

- **因为** 用户确认「统计一定会有的」（统计数据是系统确定方向，不是可有可无），**所以** 记录决策 15 并纳入 v0.2 基础范围：工作台二级「数据统计」页（已随上一重构落地页面骨架）升级为 v0.2 真实交付项——基础指标任务量/状态分布/逾期/AI 调用与成本，数据源 TM 表 + 引擎审计表（现成）；详细统计维度随版本细化，必要时提级为顶级菜单（方案 B 的「独立工作台」届时评估）。项目状态决策 15 + 建设方案 v0.2 实现内容表增第 9 项。

## 2026-08-28（v0.2 原型：任务中心改名工作台 + 二级导航 + 修导航空页 BUG）

- **因为** 用户拍板三个调整：①「任务中心」改名「工作台」（采用方案 A：工作台 = 总入口，统计数据以后作为二级加入；方案 B「新增独立工作台菜单」当前统计数据未立项、会是空顶级菜单，暂不采用，等统计立项再评估提级）；②修复「任务中心菜单点进去是空页、只有点 logo 才显示任务列表」的原型接线 BUG（原 tm 模块链接到占位页 /modules/tm 而 logo 链接 /tasks，两条路不一致）；③左侧导航预留二级菜单结构，**所以** 重构原型：MODULES 的 tm 改为带 children 的「工作台」组（任务列表 /tasks · 提案审核 /proposals · 数据统计 /stats，新增 proposals.html 全宽提案审核页与 stats.html 统计雏形页）；base.html 侧栏支持 Tabler treeview 二级导航（data-bs-toggle=collapse + nav-link-toggle，当前页所在组自动展开）；logo 与根路径统一进 /tasks；任务列表页保留任务表 + 提案侧栏双视图。Playwright 验证：工作台组展开含 3 子项、三子页标题与内容真实可达（8 任务行/3 提案/4 统计卡）、根路径重定向 /tasks、零 404。

## 2026-08-28（决策 11 修订：取消 P0-P3 优先级分级）

- **因为** 用户拍板：P0-P3 优先级分级**没有实际意义**（AI 分级不准确、人工很难界定 P0 与 P1 的区别、没有分级标准；「如果有分级的标准就 AI 根据标准分，没有就算了，以后完善时再议，现在不需要」），**所以** 修订决策 11 并全链路落地：①**契约** `models/contract/task.py` TaskProposal 移除 `suggested_priority`（P0-P3 Literal），`suggested_due_days` 保留为独立 AI 建议（截止手填 + AI 建议，人工可改，不再映射 P0=0/P1=3/P2=7）；②**文档** 项目状态决策 11、建设方案 v0.2 TM 数据模型与细则、详设 §6.4 TaskProposal 同步修订（逾期标红保留、「逾期自动升级」随分级取消；后续有分级标准则 AI 按标准分级再议，走变更日志加回字段）；③**测试** test_contract.py 移除优先级正反用例、新增 `test_task_proposal_no_priority_field` 固化契约无优先级字段；④**原型** web/app.py mock 数据与模板移除 priority（任务表去掉优先级列、提案去掉 P0 徽章与建议行优先级段、逾期提示去掉「自动升一级」）；⑤Playwright 验证：表头 任务/状态/负责人/截止/来源、页面 0 个 prio 徽章、8 任务行/3 提案/2 逾期行保留、零 404。契约「v0.1 定稿即冻结」的修订按流程走变更日志留痕（本条目即契约 version 变更记录）。

## 2026-08-28（v0.2 原型换装 Tabler）

- **因为** 用户反馈 AdminLTE 3 版本「比较丑、一点也不现代」（2019 年老式后台风格），**所以** 原型换装**现代后台模板 Tabler 1.4.0**（MIT、Bootstrap 5 底、设计语言干净现代）：vendored `@tabler/core@1.4.0`（css/js）+ `@tabler/icons-webfont@3.46.0`（ti 图标，字体路径按 CSS 相对引用归位 `css/fonts/`）到 `web/static/vendor/tabler/`（4.8MB，大陆部署不依赖 CDN）；base 壳改为 Tabler `.page`/`.navbar-vertical`/`.page-wrapper` 布局（深色侧栏 + 顶栏角色头像），任务中心改为 Tabler 卡片/表格/徽章组件，删除 AdminLTE 旧资产；Playwright 验证登录流/6 卡片/8 任务行/3 提案/5 导航/逾期红行全部就位、**零 404**。运行地址不变 `http://127.0.0.1:8090`。

## 2026-08-28（v0.2 立项）

- **因为** 用户确认 v0.1 验收全达成、决定开工 v0.2（TM 任务中心 + 引擎接通），且明确「整体系统框架优先、任务功能后议；原型优先找现成模板、没有合适的再自己动手、工具不限」，**所以** v0.2 立项并先行交付**整体系统框架原型**：新增 `web/`（FastAPI + Jinja2/HTMX，生产同栈，符合建设方案 v0.2「FastAPI 骨架 + Jinja2 模板基座」）；集成**现成后台模板 AdminLTE 3.2**（MIT，Bootstrap 4，vendored 到 `web/static/vendor/adminlte/` 约 1.9MB——大陆部署不依赖公共 CDN）；实现页面集——登录页（共享密码 + 三角色 cookie，原型形态）、应用壳（侧边栏五模块导航 TM/CRM/ERP/SEO/扒图 + 顶栏角色）、任务中心首页（4 概览卡片 + 任务清单：五态/优先级 P0-P3/逾期标红/AI 建议来源 + 提案待审核栏：风险级/依据引用/来源追溯/批准驳回，全部 mock 数据）、模块占位页；运行于 `http://127.0.0.1:8090`（nohup 后台，Playwright DOM 断言零 404）。边界遵守：`web/` 不 import engine 内部模块（lint P3-2 对 web/ 目录生效，0 违规），mock 数据不碰业务库；依赖新增 fastapi/uvicorn/jinja2。TM 详设（表结构/字段级）并行撰写中，确认后进业务开发。

- **因为** 崩溃恢复（检查点）与审计是引擎「可恢复、可审计」两大支柱（详设 §2.3/§7.3/§8；验收 A3 audit 可查、A4 崩溃恢复不重烧依赖），而它们只需在 DAO 之上做纯逻辑（DB 层 T6 并行交付），**所以** 完成 T9：`engine/core/checkpoint.py`（CheckpointDAO Protocol + CheckpointManager：write 把 phase_output+memory_state 打包进 state JSON、resume_point 取最后一条恢复定位、Pydantic 产出自动序列化不可序列化拒落、「REASON LLM 结果进 state」由测试固化支撑 A4）+ `engine/core/audit.py`（AuditDAO Protocol + AuditRecorder 三态 ok/reask/failed 都记 + DbAuditGate 实现 llm/audit_gate.py 的 Protocol（注入探活、未配置 fail-closed）支撑「先审计后调用」+ audit_summary 安全摘要：只读 task/worker/phase/result/model/token/耗时，结构性不含正文与密钥，全文只进审计表）；tests/test_checkpoint.py 10 条 + tests/test_audit.py 12 条（mock DAO 住 tests/，零网络零 DB）。技术决策：task_id 由 CheckpointManager 构造绑定、resume_point 显式收 task_id；AuditRecord.phase 为非表列（表无相位列但 §8 stdout 要求）。与详设零偏差。

- **因为** 工序/链注册表是引擎「拒载启动」的第一道闸（详设 §4 loader L1-L9；验收 A2 拒载、A8 transaction 拒载依赖），**所以** 完成 T4b：`engine/registry/schema.py`（Worker/Chain/ContextProvider/Event 四种 YAML 的 Pydantic schema，ActionDeclaration 复用 models/contract 冻结类）+ `engine/registry/loader.py`（L1-L9 全规则 + L10 Action target 锁死 tm.proposal + 事件 trigger_chain 引用一致性；公共 API `validate`/`load_registry`/`write_hashes` + RegistryLoadError 聚合违规）+ `__init__.py` 导出；tests/test_loader.py 49 条（L1-L9 每规则 ≥1 正 ≥1 反，全部 tmp 迷你仓库 + sys.modules 注入临时模型模块）。技术决策：①id 一律 snake_case（L1 规则原文 `^[a-z][a-z0-9_]*$`，详设 §4 示例的连字符/点分 id 仅为示意，T10 demo 声明须 snake_case；event_type 例外允许点分）；②L6 只做字段存在级静态检查（深类型对等留运行期 Policy 兜底）；③L9 机制 = `write_hashes` 生成 `engine/registry/model_hashes.yaml`（提交入库）记录 {model: {version, hash}}，hash = 字段名+类型稳定序列化，load 三路拒载（hash 不匹配提示 bump version / 记录缺失提示先跑 write-hashes / version 不一致）；④L4 复用 T8 的 load_models，models.yaml 缺失即拒载；⑤demo 声明 yaml（chains/context/events/actions/workers）随 T10 工序任务一并交付——依赖 models/workers 的模型类（顺序调整，本任务只交付 schema+loader+测试）。

- **因为** 引擎库 4 表与数据访问层是队列雏形/审计/检查点/A1-A4 验收的持久层（详设 §3），**所以** 完成 T6：`migrations/engine/`（alembic.ini 用 `%(here)s` 锚定 script_location + env.py async 迁移模式 + versions/0001 手写 DDL：4 表 5 索引逐列对齐 §3，含部分索引 idx_task_status 与 DESC 索引 idx_ckpt_task，downgrade 反向）+ `engine/core/db.py`（ORM 4 映射类与迁移同源 + 契约全部 13 个 DAO 函数 + 3 个 NamedTuple Row 类型；dequeue_task 用 `FOR UPDATE SKIP LOCKED` 只取不改状态）+ `tests/conftest.py`（session 级**同步** fixture：initdb 临时簇到 tests/.pgdata/ → pg_ctl → createdb → alembic upgrade head → teardown 停簇清目录，失败也清不留孤儿进程；opt-in 零影响现有测试）+ `tests/test_db.py` 23 条真 SQL（schema/JSONB 真类型/5 索引/FK、全部往返、SKIP LOCKED 锁语义、真事务回滚；测试间 TRUNCATE RESTART IDENTITY 隔离）。技术决策：JSONB 用 `postgresql.JSONB`（实测 sa.JSON 在 PG 方言渲染为 JSON 而非 JSONB，§3 类型语义锁死）；alembic async 模式（sync 引擎驱动不了 asyncpg）；strict 模式下 async fixture 必须用 `pytest_asyncio.fixture`。

- **因为** 接口契约「create_engine/env.py 从环境变量读 DB URL」与 lint P2 规则4（.py 禁 os.environ/os.getenv 读取）硬冲突——T6 按契约诚实实现（未走规避）并建议把 db.py/migrations 加进 P2 豁免区；但 db.py 恰是连接串（含密码）的高危文件，豁免会连 URL/IP 字面量检查一起关闭，**所以** 主代理改走 P2 规则4 明确允许的合法来源「.env 文件」（规则消息原文「唯一合法来源是 models.yaml 的 env: 前缀与 .env 文件」）：`engine/core/db.py` 与 `migrations/engine/env.py` 改经 `dotenv_values` 读仓库根 .env（锚定 `_REPO_ROOT`，不触碰 os.environ，P2 对 db.py 的 URL/IP 字面量检查保持生效）；alembic 注入改 `-x db_url=<url>`（实测 alembic 1.19 无旧 API get_x_argument，-x 参数存 `config.cmd_opts.x` 的 key=value 列表）；`python-dotenv` 提升为直接依赖（uv add）；修变量名 typo（`LIQUAN_ENGINE_DB_URL` → `LIUQUAN_ENGINE_DB_URL`，与 .env.example 一致）；tests/conftest.py 与 tests/test_db.py 同步更新（create_engine 回退测试改 tmp .env + monkeypatch `_DOTENV_PATH`）。

- **因为** 执行链 runner 是「命令行触发 -> 建任务入队 -> 状态机逐相位执行 -> 审计落库 -> 检查点可恢复」的核心组装件（详设 §2/§2.3/§4.2；验收 A1/A3/A4 依赖），**所以** 完成 T12a：`engine/core/context.py`（EngineContext——P3-1 工序签名 ctx 类型）+ `engine/core/runner.py`（PhaseLine/TaskResult/TaskRunner：组装 T5 状态机+policy、T8 call_llm、T9 CheckpointManager/AuditRecorder/DbAuditGate、T6 DAO；run = create_task→dequeue SKIP LOCKED→逐步状态机循环，每相位转换前落检查点（REASON 成功的 LLM 结果进 state["phase_output"]，A4 不重烧 token），resume = 取最后检查点从 to_phase 续跑，request_pause 与崩溃恢复同路径）+ `tests/test_runner.py` 14 条（A1 全相位 DONE+流水、捕获桩 prompt 组装 {input.*} {config.*}、失效桩重试 3 次 FAILED 无残留、坏输出 FAILED、空输出 VERIFY 拦截、A4 恢复不重烧且审计条数不变、PAUSED 暂停/恢复、三步链暂停续跑、无检查点报错、INIT 风险越限 FAILED 不调 LLM、链入参校验不入队、async run、审计 gate 拒绝）+ `tests/test_acceptance.py` A1-A10 共 10 条（全部 @pytest.mark.version_acceptance，A11 除外——真模型冒烟待用户 key）。契约扩展（纯追加）：db.py 新增 `StepRow` + `get_step`（OBSERVE 读回 v0.1 语义 = get_step 确认 output 已落库；resume 恢复定位用），test_db 追加 3 条。技术决策：OBSERVE 读回失败按 event=None 直接终态（转换表无该行，§2.1 读不回 = FAILED）；VERIFY v0.1 断言 = 输出非空 + output Model 校验；REASON 对 call_llm 结果做二次校验（桩不代做 output_type 强制）；审计 model 字段 = models.yaml 实际模型串（A7）；迷你仓库经 `engine.registry.__path__` 追加挂载避免影子化真包；已知边界（fail-closed）：resume 遇终态检查点极窄窗口抛 PhaseTransitionError。

- **因为** CLI 是把引擎能力暴露给命令行用户的唯一入口（详设 §10；绿 2 registry-check 与绿 4 verify 转真的落点），**所以** 完成 T12b：`engine/cli.py` 五命令实现（registry-check [--write-hashes] / run / resume / audit / verify，退出码 0 成功/1 业务失败/2 argparse usage 错保持；registry-check 打印工序/链/事件/Action 清单 + [L#] 违规行；run/resume 输出 `task e-<6位> created` + [INIT]/[REASON]/[ACT]/[OBSERVE]/[VERIFY]/[DONE] 相位流水；audit 用 T9 audit_summary 安全摘要；verify = subprocess 跑 lint+registry-check+pytest 出四绿报告）+ 仓库根 `models.yaml`（§7.1 模型注册，仅 env: 前缀零真值，P3-3 通过；L4 校验位）+ `engine/registry/model_hashes.yaml`（--write-hashes 生成，L9 登记空册）+ `tests/test_cli.py` 19 条（registry-check 4 / run-resume 接线 5 / audit 2 / verify 3 / 基础 5；run/resume 用桩 TaskRunner 锁接线，真端到端归 T12a）。技术决策：runner 惰性导入（registry-check/audit/verify 不依赖 runner）；CLI 启动期 _load_dotenv（.env 装载，P2 规则4 合法来源）使 models.yaml env: 引用可解析；writable_check = 启动期 SELECT 1 探活后注入 lambda: True；仓库根自 cwd 向上找 pyproject.toml 锚定。集成（主代理）：`tests/test_cli.py::test_run_missing_dotenv_clear_error` 因本机已有占位 .env 而红——补 monkeypatch `engine.core.db._DOTENV_PATH` 指向不存在文件（测试自足）；本机建占位 `.env`（gitignored，DEEPSEEK_API_KEY/BASE_URL/LIUQUAN_ENGINE_DB_URL，A11 前替换真 key）。

**Wave 3 里程碑**：四绿总门**全部转真**（绿 2 registry-check 真跑、绿 4 verify 真跑，零 skipped）；验收断言 **A1-A10 全绿**（`pytest -m version_acceptance`）；全量单测 318 通过。

- **因为** v0.1 需要首批真实工序验证内核（详设 §14 拍板：demo-echo 桩驱动冒烟 + 翻译雏形为 v0.3 探路），**所以** 完成 T10：`models/workers/`（DemoEchoInput/Result、ChatTranslateInput/Result、DemoGreetingParams/DemoGreeting、DemoEchoEventPayload 共 7 个 pydantic v2 Model）+ `engine/registry/workers/demo/demo_echo/` 与 `crm/crm_translate/`（worker.yaml P3-1 契约 + run.py + schema.py re-export + prompt.md + config/style.yaml 规格外置——demo_echo ACT 确定性回显、crm_translate 消费 ctx.llm_output 原样落库）+ 链声明 `chains/demo/demo_echo_chain/`、`chains/crm/crm_translate_chain/` + 登记位 `context/demo.yaml`（demo_greeting）/`events/demo.yaml`（demo.echo_done → demo_echo_chain）/`actions/demo.yaml`（demo_echo_record，target 锁死 tm.proposal）+ `model_hashes.yaml` 重生成（4 model 4 条）+ `tests/test_workers.py` 12 条（模型往返、两 run 直调正反、真声明 load_registry、**真 worker 全链路**两条链：嵌入式 PG + FakeAgent + 真注册表 + TaskRunner → DONE + 全相位流水 + 审计 1 条零网络）。技术决策（T10 实测定型）：①链 id 因 L1 跨表全局唯一不能与工序 id 同名（demo_echo_chain/crm_translate_chain）；②链文件须 `chains/<域>/<链>/chain.yaml`（loader 口径）；③工序目录名必须等于工序 id（runner 按 `engine.registry.workers.<域>.<id>.run` import）；④context provider id 点分被 L1 拒（id 用 demo_greeting，provider 字段保留 demo.greeting 实现标识）；⑤翻译链入参须显式给语言字段（runner 用原始 input dict 渲染 prompt，Model 缺省值不反填）。**目录矛盾收敛**：工序目录 T10 定型为 `engine/registry/workers/`（详设 §4.1/§11 口径），详设 §9 对象行与 P3-2 判定式、开发规范 R24 判定式、p3.py docstring 同步更新（`engine/workers/` 旧口径废弃，lint 双扫兼容存量）；test_cli 真仓库断言由空册更新为 2 工序 2 链 1 provider 1 事件 1 Action。

- **因为** T10 工序落地后 P1 自动词表会与真实 config/ 交互（run.py 按 R10 设计机制用 `ctx.config["tone"]` 读配置——键字面量会被键入表打成违规；chain.yaml 的 worker id `demo_echo` 会被 config 值 "echo" 子串命中误伤），**所以** T11 收紧（2026-08-28，T10 前置）：`engine/lint/p1.py` 词表**只取 config 标量值**（键不入表——按键读取配置是 R10 设计机制）；纯 ASCII 词按**整词匹配**（前后非词字符，"demo_echo" 里的 "echo" 不算命中；中文专名仍子串匹配，CJK 无词边界）；tests/test_lint_p1.py 键拦测试翻转为反向 + 新增整词正反回归。

- **因为** 集成验证发现 runner 的 ACT 相位拿不到 REASON 的 LLM 结果（EngineContext 无该字段）——demo-echo 确定性重算可掩盖，但翻译雏形必须消费 LLM 结果才能执行副作用（详设 §2.1：REASON 产出 -> ACT 执行），**所以** 集成修复（2026-08-28）：`engine/core/context.py` 的 EngineContext 增 `llm_output` 字段（REASON 结果，默认 None）；`engine/core/runner.py` 的 `_phase_act` 把当前相位产出（REASON 的 LLM 结果）经 ctx.llm_output 传给 run()（避免工序二次调 LLM，R4）；tests/test_runner.py 增 `test_act_worker_receives_llm_output` 固化（FakeAgent 输出到达 ACT 且全程只调 1 次 LLM）。

- **因为** 用户提供了 DEEPSEEK_API_KEY 真值（2026-08-28，替换 .env 占位，R20 值只存 .env 不入 git），**所以** 执行验收断言 A11（真模型冒烟，详设 §12）：`liuquan-engine run crm_translate_chain --input '{"text":"hello world","source_lang":"en","target_lang":"zh"}'` 真调 DeepSeek 一次——全相位到 DONE（1.2s），审计真值落库（model=deepseek-chat、tokens_in=407、tokens_out=75、duration_ms=933），译文「你好，世界」经 engine_step.output 落库；成本 ≈ ¥0.0006 远小于 0.01 元阈值。**验收断言 A1-A11 全部达成，v0.1 代码侧与验收侧完成，待用户复核后 git tag v0.1。**

## 2026-08-28（代码审查修复批次）

- **因为** 用户要求全代码 review，4 个审查子代理分系统（执行链/注册表与契约/lint+CLI/测试有效性）对 v0.1 全部代码做了只读审查，发现真实集成层缺陷（桩+正确注入掩盖了 CLI↔runner 接线问题），**所以** 批量修复（每条先复现核验再修）：
  - **C1（critical，三审查员交叉确认）**：`engine/cli.py` run/resume 漏传 `model_registry`/`repo_root` 给 TaskRunner——真命令必在 REASON 抛错且任务卡 running 脏行；补两个注入参数 + `runner.run()/resume()` 加意外异常兜底（任何未捕获异常也落终态，不留 running 脏行）。端到端验证：`liuquan-engine run demo_echo_chain` 现在走到 [INIT]→[REASON]→[FAILED] 正常终态。
  - **M1**：resume 后 `ctx.llm_output` 是检查点还原的 dict 非 Model（真实翻译工序 isinstance 校验必 FAILED）——`_phase_act` 里用 `_coerce_model` 归一化为 output Model 再进 ctx（A4 对真实工序的续跑前提）。
  - **M2**：ACT 重试 off-by-one（max_attempts=2 只重试 1 次）——`_transition_ctx` 的 attempt_left 改 `max_attempts + 1 - attempt`（1 初调 + 2 重试 = 3 次尝试，与 LLM 层 range(max_attempts+1) 同口径）。
  - **M3**：崩溃落在「步骤 VERIFY→DONE 终态检查点 → 任务终态」窗口时 resume 目标 DONE 抛错——resume 遇 DONE 目标改为续跑后续步骤/终态（输出已落库），删除死代码 `_resume_target`。
  - **M7**：context provider 域按 id 点分前缀推导（snake_case id 如 demo_greeting 被 Policy 误拒越域）——改从 `registry.context_providers[id].domain` 声明域解析（与 loader L2 同源）。
  - **M8**：孤儿任务（create 后未取走即崩溃）毒化队列——run() 的 dequeue 遇非本任务先标记 failed 清理再重试（有界 20 次）。
  - **注册表 M1**：L9 `_canonical_type` 非规范化（`Optional[str]`/`str|None`/`Union[str,None]`/`None|str` 三 hash，Literal 顺序敏感）——规范化（Union 去 NoneType 统一 Optional 形态、Literal 排序、Annotated 剥离），model_hashes.yaml 重生成，回归测试断言等价注解同 hash。
  - **注册表 M6**：`registry-check --write-hashes` 在非 L9 违规时仍改写登记文件——改为有非 L9 违规即跳过（L9 mismatch 恰是 write-hashes 要修的，不拦）。
  - **测试基础设施**：major-1 test_cli 向共享 PG 簇插入任务不清理 → 污染 test_runner/test_acceptance（`pytest tests/test_cli.py tests/test_runner.py` 必红）——TestAuditCommand 加每测 TRUNCATE；major-2 conftest 被 kill 后遗留孤儿 PG 进程与目录——fixture 启动时清理全部 `tests/.pgdata/pgtest-*`（含不同 pid）；major-3 暂停测试「不重烧」断言不足——补 `len(agents)==1` + 审计条数双锁。
  - **新增回归测试 6 条**（test_runner +5：resume llm_output 归一化/ACT 三次尝试/DONE 窗口恢复/provider snake_case 域/孤儿清理；test_loader +1：等价注解同 hash）+ test_cli 桩签名补 model_registry/repo_root + resume 打印 verb=resumed + 恒真式断言加固。
  - **文档化偏差（不改代码，v0.2 前评估）**：M4 队列原子认领（dequeue 只取不改，双消费者可重取同一任务——v0.1 单消费者无影响，v0.2 常驻 runner 需改原子认领）；M5 审计 reask 粒度（re-ask 在 PydanticAI 内部 retries 参数，runner 每次 REASON 尝试记一条 ok/failed，无 result=reask 条目）；M6 工序重试单行原地累加 attempt（§3 表注释「重试=新行」未落地）；M9 暂停消耗 ACT 重试预算；L9 软门（write-hashes 不 bump version 可洗白 Model 变更）；共享 Model 多工序 version 一致性；L6 只做字段存在级静态检查（§4.2 类型对等留运行期 Policy）；validation.py 两条业务校验无生产调用点（suggest 工序落地时补 runner 钩子）；provider id 点分被 L1 禁（v0.3 用 snake_case id + provider 字段点分实现标识）；P2 URL 判据窄于 §9 原文（只 http/https scheme + IPv4）；SELECT 1 探活≠审计可写（§7.3 先审计后调用弱化）；占位 .env 无守卫（A11 前替换真 key）。
  - **环境说明**：本会话后期沙箱 I/O 整体变慢（原始代码同样从 12s 涨到 ~59s），测试耗时增加为环境所致非代码回归；全量 339 通过（原 333 + 新 6 条）。

## 2026-08-28

- **因为** v0.1 后续任务（迁移/队列/审计/检查点/A1-A4 断言）依赖 PG16 引擎库与 Pydantic/PydanticAI/SQLAlchemy 运行依赖，而本机无系统 PG16（实测确认：无 docker、sudo 需密码、无 /usr/lib/postgresql、5432/5433 无监听），**所以** 完成 T3（环境与依赖）：`uv add` 运行依赖 pydantic 2.13.4 + pydantic-ai 2.35.1 + sqlalchemy 2.0.52 + asyncpg 0.31.0、dev 依赖 pytest-asyncio 1.4.0 + alembic 1.19.1（技术定：引擎 asyncio 化——Context provider 为 async def，DB 走 SQLAlchemy 2.0 async + asyncpg，CLI 用 asyncio.run）；PG16 无 root 部署（apt download postgresql-16/postgresql-client-16/libpq5 → dpkg -x 解压到 `~/.local/pg16`，LD_LIBRARY_PATH 指向解压 libpq，initdb 以当前用户起簇于 5432，建 liuquan_engine 库）并交付运维脚本 `scripts/pgdev.sh`（start/stop/status/initdb/psql/createdb，不 import engine，符合 R24）；`.env.example` 增 `LIUQUAN_ENGINE_DB_URL` 变量名（R20 只留名，DB URL 不进 git）。测试嵌入式簇（`tests/.pgdata/`）由 T6 conftest 落地。

- **因为** 业务模块接引擎的唯一接口必须先冻结（详设 §6 四契约，v0.1 定稿即公共契约），**所以** 完成 T4a：`models/contract/` 四契约落位——`event.py`（ContractEvent 六字段）、`action.py`（ActionDeclaration/ActionResult，risk Literal 天然拒 transaction）、`task.py`（SourceTrace/EvidenceRef/TaskProposal，evidence 上限用 pydantic v2 `Field(max_length=20)`——详设 §6.4 的 max_items 是 v1 语法）、`context.py`（ContextProvider @runtime_checkable Protocol：`async def provide(params) -> Model`，只读供给无副作用）；`validation.py` 放 §6.4 两条业务校验（无依据不出建议 assert_suggest_has_evidence / audit_ids 可追溯 validate_audit_ids 注入 lookup），模型层保持纯声明；tests/test_contract.py 36 条正反测试。技术决策：ActionDeclaration.target 保持 str 不锁 Literal（「一期唯一合法值 tm.proposal」由 T4b loader 校验执法，契约不重复执法）。

- **因为** 状态机（相位转换代码写死，AI 无权决定下一步）与域权限门禁是引擎执行链的地基（验收 A1 相位流水、A8 风险门禁依赖），**所以** 完成 T5：`engine/core/statemachine.py`（Phase 枚举含 PAUSED 非终态、§2.2 转换表 TRANSITION_TABLE 嵌套 dict 写死、transition(current, event, ctx) 守卫、12 事件常量、PhaseTransitionError——事件决定表行、ctx 的 frozen dataclass 决定分支：output_invalid 读 reask_remaining、side_effect_failed 读 attempt_left、resume 读 resume_phase，条件矛盾/缺失 fail-closed；落盘由 runner 负责）+ `engine/core/policy.py`（Risk 四级 + transaction 恒拒、§5.2 六行矩阵全定义仅 demo/crm 启用、check_policy 四路拒：transaction/域未启用/越域读/风险越限）；tests/test_statemachine.py 31 条 + tests/test_policy.py 11 条正反测试。与详设零偏差。

- **因为** LLM 调用是引擎唯一 AI 出现点，必须先立好 PydanticAI 封装（详设 §7/§13/§14），**所以** 完成 T8：`engine/core/llm/` 四文件——`models_config.py`（models.yaml 加载 + ModelRegistry，env: 前缀解析，拒载规则 7 类：真值形态/缺字段/重复别名/坏 yaml/缺 env 变量/空 models/未知 provider）、`agent.py`（`agent_factory(registry, alias, output_type, *, agent_cls=None)` 即 §13 构造注入点——真路径建 PydanticAI Agent，桩按契约 `(config, output_type, reask_limit)` 构造共用 run 契约零 monkeypatch；`call_llm` 三级策略：re-ask 2 次（PydanticAI `run(retries=N)` 原生能力）/ 网络错误超时 5xx 重试 2 次指数退避 / 耗尽显式异常无静默降级；单次超时 30s 双保险）、`audit_gate.py`（先审计后调用 §7.3）；tests/test_llm.py 31 条 + 8 桩家族住 tests/。技术决策（均入 docstring）：①退避「5s→30s」实现为 `min(5·2^k, 30)` 指数增长封顶；②provider/model/base_url/api_key 必填，timeout_s/reask_limit 缺省 30/2（§14 值）；③env: 引用的变量未设/为空即拒载（fail-fast 与「拒载=引擎不起」同哲学）；④真路径 v0.1 仅 deepseek（OpenAI 兼容），其他 provider 显式抛 LLMError；⑤审计 gate 逻辑调用前查一次。实测坑：pydantic-ai 默认 HTTP 客户端读环境代理（本机 ALL_PROXY=socks:// 致构造失败），真路径显式 `httpx2.AsyncClient(trust_env=False)` 解耦。

- **因为** 硬编码四层防线的机器层缺 P1 业务词黑名单（详设 §9；词表 = 基础词表 ∪ 自动扫 config/ 生成，重复扫自动生长），**所以** 完成 T11：`engine/lint/p1.py`（P1BusinessTermsRule：扫描 engine/workers/ 与 engine/registry/ 下 .py（AST 字符串常量含 docstring）与 .yaml（compose 节点树标量值带行号）；自动词表每次 check 无状态重扫各工序 config/ 的键与值（布尔/None/长度<2 过滤）；config/ 自身豁免；解析失败 fail-closed；子串匹配保守取向防漏）+ `engine/lint/__init__.py` RULES 注册一行（框架 rules.py 未动）；tests/test_lint_p1.py 17 条正反测试（全部 tmp 迷你仓库，真仓库 0 违规断言）。集成收尾（主代理）：`engine/lint/__main__.py` 删「T11 落地后加入」过时文案；`tests/test_lint_p3.py::test_rules_registry_shape` 期望列表补 "P1"（注册新规则的必然结果）。当前仓库无 config/ → 自动词表为空，T10 工序 config/ 落地后自动生效（Wave 4 验证）。

## 2026-08-26

- **因为** 对 EOMS 初版不满意（三个孤立工具无循环、AI 可用性依赖本地广成电脑开机），决定重构为综合智能运营系统（AI 大管家），**所以** 建立本仓库并初始化 git；完成三轮现状摸排（广成引擎 8500 行/444 单测、EOMS 112 单测/CRM 端到端 5.8s、NocoBase 生产 ERP 134MB/152 表），校验 GPT 概设 v0.2 并修正三处失真。
- **因为** 与业务负责人逐项讨论定稿（10 项决策拍板：新建引擎取广成之骨换 PydanticAI 之心、分层统筹、PG16 双 database、阿里云部署、ERP 只读增强、EOMS 退役、全免费技术栈、权限从简、AI 100% 可控为最高原则），**所以** 产出概设 v0.3（`概要设计-v0.3.md`），业务方审阅通过。
- **因为** 项目需要让任何 AI 会话自动获得上下文，**所以** 创建根目录 `CLAUDE.md`。
- **因为** 要给设计期留档、给后续详设和开发立规矩，**所以** 建立 `docs/` 文档体系：`变更日志.md`（本文件，照抄广成留痕原则）、`开发规范.md`（可判定规矩，原则承广成、内容为刘全新写）、`版本规划.md`（应用户要求取代"路线图与待办"形态，版本 = 能力里程碑不当待办容器）；概设 v0.3 从 `docs/superpowers/specs/` 挪到 `docs/` 根，v0.2 草稿归档，删除空的 `NEW-MD.md`。本条为文档层改动，未动代码。
- **因为** 文档体系缺两块：版本规划只给"大概实现什么"，没有对应的详细实施内容（做什么/怎么做/完成形态/验收）；也没有一个只反映当前状态、与项目保持一致的文件，**所以** 新增 `docs/建设方案.md`（与版本规划配套：每版详细实施内容，含总技术底座与跨版本依赖，⚑ 标详设待决策点）和 `docs/项目状态.md`（唯一状态源：当前阶段/版本进度/已拍板决策/下一步/外部依赖，状态变化即更新）；同时明确工序模式边界（回答业务方疑问）：工序只是 AI 引擎的最小能力单元（engine/workers/），业务模块不是工序、不套工序模式；README 与 CLAUDE.md 同步。
- **因为** 业务方担心开发期硬编码问题（广成 2026-08-18 审计返工的前车之鉴：光靠文档规范防不住，飞书列名/13 标签/SEO 规格当时全写死在代码里），**所以** 确立硬编码四层防线并写入文档：①架构层（工序只碰 inputs/config/engine 原语，密钥端点结构上写不进）②去处层（业务值外置四优先级）③机器层（engine/lint 三规则：P1 业务词黑名单词表从注册表自动生成、P2 凭据端点零容忍、P3 契约，配反向测试）④守门层（每条 lint 规则有反向测试防校验器改坏）。开发规范 R10 扩充为四层防线 + 验收检查表同步；建设方案 v0.1 增实现项 9（lint 工具与首批工序同步落地，不事后补）+ 验收标准增 lint 反向测试项。
- **因为** 业务方提出开发期强制问题：怎么确保 AI 按文档目标与规范执行开发（广成前车之鉴：规范写得好但 AI 开发时不读不守，2026-08-18 审计才发现大量违规），**所以** 建立开发强制机制并成文 `docs/开发流程.md`：三层防线（知道规范 -> 难以违规 -> 违规过不去）六项设置（①CLAUDE.md 入口卡内联铁律 7 条 ②详设先行卡：详设经确认才动代码 ③逐任务执行卡：TDD+四绿+日志+commit ④四绿 git pre-commit hook：scripts/check.sh 不绿不让提交 ⑤验收断言：建设方案验收标准写成可执行测试 @version_acceptance，完成 = 断言全绿+用户复核 ⑥发版前审计）；CLAUDE.md 顶部插入铁律 7 条（内联非指针，保证进上下文）；项目状态.md 增开发强制机制段。第 3 层（机器强制）是唯一真强制，与刘全自身哲学同构：AI 可以开发，脚本负责验收。

## 2026-08-27

- **因为** 凭据端点与契约纪律必须在首个工序代码落地前由机器执法就位（规范 R10 四层防线的机器层、R12/R24 的 grep 判定式要机器化，且 T2 在 T10 工序任务之前），**所以** 完成 T2：交付 `engine/lint` 可执行包（`python -m engine.lint` 扫全仓 0 违规退出 0、有违规打印明细退出 1）--规则框架 `rules.py`（Rule 统一接口 `check(repo_root) -> list[Violation]` + 全仓 .py 收集/AST 解析助手，P1 业务词黑名单 T11 插入方式 = 新规则文件 + `__init__.py` RULES 注册一行，框架不改）；`p2.py` 凭据端点零容忍（AST 检测：URL scheme/IPv4 字面量、sk- 前缀串、api_key/apikey/password/token 敏感名赋非空字面量（参数声明与传参不拦，防误杀）、os.environ/os.getenv 绕路（含 from os import 形态）；豁免 engine/core/llm/、tests/fixtures/、scripts/ 写死在规则配置；语法坏文件 fail-closed 记违规）；`p3.py` 契约纪律四条子规则（P3-1 工序 run 签名：参数名/注解/裸 dict 拦截，注解以「名字由本文件 import 绑定」为模块可解析判据，未登记 Model 真校验归 T4、EngineContext 精确类型 T9 定型后收紧；P3-2 低耦合：web/scripts 不得 import engine.workers/engine.core、工序 run.py 不得 import 其他 worker（相对 import 按 R21 解析，自身包内 schema 合法）；P3-3 models.yaml api_key 非 env: 前缀即违规 + 全树 sk- 直值兜底，依赖新增 pyyaml（compose 节点树带行号，T4 loader 同源复用）；P3-4 生产目录 Fake*/Stub* 前缀类零容忍、tests/ 豁免，判据与规范 R12 判定式一致）；`tests/test_lint_p2.py` + `tests/test_lint_p3.py` 共 48 条正反测试（tmp_path 迷你仓库树不污染真仓库，反向样本必须被拦；真仓库含测试文件全绿断言）；check.sh 绿 1 删 skipped 分支转真跑。**已知判据边界**（文档化在各规则模块 docstring）：lint 自身检测前缀运行期拼接构造防自检红（开发中实锤抓到一次 docstring 示例 IP 自检红，证明机制有效）；AST 只扫字符串常量（docstring 在内）不扫 # 注释；敏感名子串匹配对 tokenizer 类命名可能误报（此类代码应住 engine/core/llm/ 豁免区）；**详设 §4.1/§11 的工序目录写 engine/registry/workers/ 与 §9/规范 R24 的 engine/workers/ 不一致（存量文档矛盾，非本次引入）**--P3-1/P3-2 两处目录与两种 import 前缀都拦，T10 定型后收敛。项目状态/README/CLAUDE.md 状态行同步。

- **因为** v0.1 详设已定稿、开发启动，四绿守门（规范 R13/开发流程 ④）不能停留在文档上必须机器强制（承广成教训：第 3 层才是唯一真强制），**所以** 完成 T1：把「尚无代码的设计期仓库」变成「uv 项目骨架」--`pyproject.toml`（name=liuquan、Python>=3.12、console_scripts 注册 `liuquan-engine = engine.cli:main`）+ engine/models/migrations/tests 目录就位（core/registry/lint 等留空包，不提前实现后续任务）+ 最小 `engine/cli.py`（argparse，仅 --version 与无命令打印用法退出 0，§10 真命令后续任务落地）+ `.env.example`（只留变量名）+ `.gitignore`（.env/.venv/缓存/pgserver 数据目录）；交付 `scripts/check.sh` 四绿总门（绿 3 单测真跑、其余未建项 skipped 记绿并注明落地任务号、任一真红非零退出、末尾四行绿/红汇总、不 import engine）并挂 git pre-commit hook（`git config core.hooksPath scripts/hooks`，跟踪文件即生效 hook）；`tests/test_skeleton.py` 四条冒烟保单测真绿。项目状态同步为「v0.1 开发中」。R25 完工交叉 review 补刀：CLAUDE.md 与 docs/README.md 的当前阶段行仍写「设计期/尚无代码」，一并同步为「v0.1 开发中（T1 已落地）」。

- **因为** 2026-08-27 全文档 review 实锤抓出"引用不存在之物"（P3 规则 4）与 CLI 位置矛盾等 12 处交叉不一致，证明此类缺陷机器校验覆盖不到、单文档自查看不见，且业务方要求把 review 固化强制，**所以** 开发规范新增 R25（完工交叉 review）：每任务四绿后标完成前强制做，范围收窄两类（文档同步 review：状态文档过期与规则号/断言号/表名引用存在性；详设↔代码 review：断言逐条有落点、文件清单逐项有交代、偏差记变更日志），明确不做风格 review 与自评（四绿和铁律 7 已覆盖），review 结果一行进 commit message；验收检查表与开发流程逐任务执行卡同步；各文档规矩范围表述统一为 R1-R25。

- **因为** v0.1 详设的 2 项待拍板已确认（审计数据永久保留；演示工序 demo-echo + 翻译雏形），**所以** 详设 v0.1 定稿（⚑ 清零），验收断言补 A11（真模型冒烟：审计真值落库、单次成本 < 0.01 元）。
- **因为** 详设定稿后对全文档做整体 review，发现 12 处不一致，**所以** 全部修复：①P3 规则 4（桩泄漏）被 A9/§13 引用但未定义（此前批量替换静默失败）——补上；②详设 §8/头部残留"待拍板"措辞与 R1-R11 旧范围、loader L1-L8 旧校验数——同步为已拍板/R1-R24/L1-L9；③CLI 入口位置自相矛盾（P3 规则 2 禁 scripts import engine，而 CLI 必须调 engine）——CLI 移入 engine/cli.py，scripts/ 只留运维脚本；④规范 R15 版本格式（x.y.z）与详设示例（整数）不一致——统一为整数递增；⑤版本规划"需业务输入"表中 Business AI Contract 未标已定；⑥概设 §8 待办表未注 #1/#2/#3/#5/#6 已由详设落定；⑦项目状态/README/CLAUDE.md 的"待确认"状态过期——全部同步为定稿可开发。另发现文档中箭头字符为 Unicode `→` 与显示不一致（此前 Edit 反复失败根因），已知问题不批量改动。

- **因为** 详设 v0.1 的 2 项待拍板决策已由业务方确认，**所以** 落定：审计数据永久保留（清理策略一期不做，量大再议）；演示工序定 demo-echo（桩驱动冒烟）+ 翻译雏形（真调 DeepSeek，v0.3 探路）。详设 v0.1 ⚑ 全部清零，**详设定稿，v0.1 可进入开发**；验收断言补 A11（真模型冒烟：审计真值落库、单次成本 < 0.01 元）。

- **因为** 业务方提出低耦合必须是贯穿全系统的总则并强制执行（不仅功能/数据/模型三者之间，一切部件配对皆然；扩展 4->8 块、换模型、换 ERP 数据源的成本都由耦合度决定），**所以** 开发规范新增「二B 低耦合总则」R21-R24：R21 耦合三通道（依赖只许契约/声明/配置三形态，附全系统耦合矩阵 10 配对）、R22 Model 唯一通用语言（结构漂移活不过启动）、R23 关系进声明不进代码、R24 机器执法（lint P3 增 worker 间 import 与 web 越界 import 两条子规则，每条配反向测试）；验收检查表增 3 项；详设 v0.1 P3 规则 2 扩充、loader 增 L9（Model hash 与 version 联动）、验收断言增 A10、§13 补 Context 通道桩与数据三通道策略表（Context 走配置切换/LLM 走构造注入/ACT 走真测试库）；CLAUDE.md 铁律 6 同步。
- **因为** 业务方询问测试数据与测试规则是否已入规范，并明确倾向广成的注入式桩模式（SkillExecutor 构造注入 + 变体桩家族 + lint 原则3 桩只住 tests/ 的机器执法），原规范仅 R12 一句话不够可判定，**所以** 把广成桩模式提炼为刘全测试规范：开发规范 R12 扩充（桩注入式/桩只住 tests/）并新增 R12 实施细则节（注入点唯一、桩家族五件套：标准/捕获/失效/坏输出/空输出各断一条路径、测试数据四规则、grep 判定式），验收检查表同步 3 项；详设 v0.1 lint P3 增规则 4 桩泄漏、新增 §13 测试桩设计（FakeAgent 家族定义与六条关键路径）、验收断言增 A9。生产代码零桩代码、测试零网络由机器执法，不再靠自觉。
- **因为** 详设启动（项目状态"下一步"第 1 项），按概设 §8 问题清单逐项落实：四契约字段级、引擎表结构、工序 YAML schema、域权限矩阵、PydanticAI 重试降级、审计脱敏、lint 三规则细则，**所以** 产出 `docs/详设-v0.1-引擎地基.md`（v0.1 动代码的唯一依据）：状态机相位转换表（AI 仅 REASON 相位、转换代码写死）、引擎库 4 表 DDL（engine_task/step/checkpoint/audit）、工序/链/Context 五种 YAML schema 与 loader 八条校验规则、四契约 Pydantic 字段级定义（TaskProposal 必带 SourceTrace+EvidenceRef，无依据不出建议）、模型注册 env: 前缀 + 无静默降级（宁失败不假结果）、验收断言 A1-A8；留 2 项业务拍板（审计保留时长、演示工序选择）。README/项目状态/CLAUDE.md 同步，v0.1 待业务方确认后进开发。
- **因为** 与业务负责人确认 AI 统筹深度（预定义链 + 任务优先级即可，不做跨域每日重点/自主调度）与数据视野节奏（v0.6 再接 ERP 视图），**所以** 保持概设 v0.3 分层统筹决策与六步顺序不变，仅记录确认（决策 14）。
- **因为** 业务拍板「全部 AI 提案必须人工审、不设代码自动通过」（比原「低风险自动通过」更保守，与 EOMS 待办预览确认先例一致），**所以** 修订 `开发规范.md` R9、`概要设计-v0.3.md` §3.4/§4.1、`建设方案.md` v0.2 审核流与验收、`版本规划.md` v0.2 验收：TaskProposal 一律进审核页人工批/驳，风险级仅作展示与排序（决策 12）。
- **因为** 业务拍板 TM 细则（优先级 P0-P3 四级+逾期自动升级超期标红；按角色指派、AI 提案按域带默认角色可改；截止手填+AI 建议 P0 当天/P1 三天/P2 一周可改；状态五态 open/in_progress/done/void/blocked；循环任务统一交 v0.4 定时链、TM 不存循环模板），**所以** 更新 `建设方案.md` v0.2 TM 数据模型条目，`版本规划.md`「需业务输入」表移除 TM 细则项，概设 §7 新增决策 11（决策 11）。
- **因为** 业务拍板 SEO/流量数据来源用 eHunt 能拿到的（流量维度一期不做），**所以** 更新 `建设方案.md` v0.5 listing 体检定时链数据来源已定 eHunt，频率仍待拍板（决策 13）。
- **因为** 需要给接手开发的 AI 一份「强制守规范 + 反幻觉」方法论与两项目实际遵守审计的可操作手册（本次实测：广成 444 单测全绿/113 registry 全过但 lint 词表仅 5+8 词致 98 处业务词放行、verify 带 skip 旗标与其规范原则 18 矛盾；刘全 pre-commit hook 已生效但四绿 3/4 为诚实 skipped），**所以** 新增 `docs/方法论报告-AI可控与反幻觉.md` 并登记进文档索引，供任何 AI 会话接手时直接引用。
