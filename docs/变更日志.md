# 刘全变更日志

> 只追加，不删除历史。按日期倒序（最新在上）。
> 格式：**因为** {原因}，**所以** 把 {旧} 变成 {新}

---

## 2026-08-26

- **因为** 对 EOMS 初版不满意（三个孤立工具无循环、AI 可用性依赖本地广成电脑开机），决定重构为综合智能运营系统（AI 大管家），**所以** 建立本仓库并初始化 git；完成三轮现状摸排（广成引擎 8500 行/444 单测、EOMS 112 单测/CRM 端到端 5.8s、NocoBase 生产 ERP 134MB/152 表），校验 GPT 概设 v0.2 并修正三处失真。
- **因为** 与业务负责人逐项讨论定稿（10 项决策拍板：新建引擎取广成之骨换 PydanticAI 之心、分层统筹、PG16 双 database、阿里云部署、ERP 只读增强、EOMS 退役、全免费技术栈、权限从简、AI 100% 可控为最高原则），**所以** 产出概设 v0.3（`概要设计-v0.3.md`），业务方审阅通过。
- **因为** 项目需要让任何 AI 会话自动获得上下文，**所以** 创建根目录 `CLAUDE.md`。
- **因为** 要给设计期留档、给后续详设和开发立规矩，**所以** 建立 `docs/` 文档体系：`变更日志.md`（本文件，照抄广成留痕原则）、`开发规范.md`（可判定规矩，原则承广成、内容为刘全新写）、`版本规划.md`（应用户要求取代"路线图与待办"形态，版本 = 能力里程碑不当待办容器）；概设 v0.3 从 `docs/superpowers/specs/` 挪到 `docs/` 根，v0.2 草稿归档，删除空的 `NEW-MD.md`。本条为文档层改动，未动代码。
