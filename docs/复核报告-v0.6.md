# v0.6 复核报告（扒图重做 · 素材库落地：图集层雏形）

> 对照操作路径，逐项验证。结论栏填「通过 / 需修改」。
> 复核人：用户（业务负责人）  日期：2026-09-03

## 一、验收断言 A1-A75（`uv run pytest -m version_acceptance` = 84+ 全绿；全量 **885 passed 五绿全绿**，planned 缺口清零）

| # | 断言 | 落点 |
|---|---|---|
| A57 | 立即扒端到端：贴链接 → 下载链真跑 → link_record done + image_file 挂链接（宽高/水印/source_mark=scraped）→ 素材库可见 | test_acceptance_v06::test_a57 |
| A58 | 拆两链 + input 契约（download=ScrapeChainInput / suggest=SuggestionInput），web 提交不 422 | test_a58 |
| A59 | 一链接一条 + url 幂等（normalized_url 唯一；队列去重 + links POST existing） | test_a59 |
| A60 | 一链接一文件夹（图片 01.xxx 归集命名 + meta.txt）+ zip 导出移除（404） | test_a60（改语义） |
| A61 | 图片来源标记 source_mark 默认 scraped + CHECK 四值 + sku_id/shop_id 留位（迁移 0011/0012 结构断言） | test_a61 |
| A62 | 定时队列：种子 → from_queue 真跑 → 建记录下载落库 → 队列清空 → done 重跑跳过 | test_a62 |
| A63 | 定时默认时间设置生效（改键 → 定时页 cron 变化，update_time 实锤） | test_a63 |
| A64 | xhs 连接器照广成（from source import XHS + explore_data 中文列 + 差集 + 未落盘 xsec_token 提示 + vendor 完整度） | test_a64 |
| A65 | 闲鱼连接器照广成（10+6 选择器 + naturalWidth + 10 过滤词 + 失效页删图 + 全局 90s deadline） | test_a65 |
| A66 | suggest 链诚实化：REASON 真调 LLM + SuggestionResult 校验 + evidence 白名单 + 提案 pending（保留资产） | test_a66 |
| A67 | EngineContext.biz_client 注入 + image_inspect 读路径 + PATCH 写回 | test_a67 |
| A68 | 失败/降级明确提示（failed+error_note / degraded_note 页面可见） | test_a68 |
| A69 | 一链接一文件夹 + meta.txt 内容齐全（来源/原链接/作者ID 缺失记「无」/…） | test_a69 |
| A70 | 夸克上传流程：upload_netdisk=true → netdisk_status=uploaded + url 回填 + meta.txt 网盘行更新；false → none 零调用 | test_a70 |
| A71 | 未授权/上传失败 → failed + error_note 含登录提示（页面可见） | test_a71 |
| A72 | 历史补传入口（done 未上传按钮显示 / uploaded 隐藏 / create_task 补传链入参） | test_a72 |
| A73 | 定时队列在扒图页（/scrape 含队列块；设置页不再含） | test_a73 |
| A74 | netdisk.upload_default 生效（改 false → 页面复选框初始值 false + 定时 input false） | test_a74 |
| A75 | 独立任务详情页 GET /tasks/{id}（完整内容/来源依据/事件时间线/步骤 + 越界 404） | test_a75 |
| 回归 | A1-A45（v0.1-v0.4）+ A46-A56（v0.5）+ B5 收口 A29/A47/A48/A51（真测试） | 全绿 |

> B5 收口（假绿根因）：A47（贴链接→落库→提案审核真实链路）/ A48（提案批准→tm.task domain=scrape + 扒图徽章）/ A51（seo-optimize 规格 config 可改实锤）/ A29（CRM 候选忽略 dismissed 不建任务不飞书）——planned → implemented 真测试，假绿清零。

## 二、页面复核操作路径（http://127.0.0.1:8090，登录角色 ops）

1. **扒图页（SEO → 扒图）两区块**：区块 A 贴链接（多行 + 立即扒 + 加入定时队列 + 当前队列条数/查看/清空 + 定时默认时间提示）；区块 B 素材库列表（一链接一条：来源徽章 📕/🐟/🔗 · 状态徽章 · 图数 · 元数据 desc/tags/作者 · 批次 · 时间 · 失败原因灰字 · 筛选 + 分页）
2. **立即扒**：贴真实小红书分享链接 → 图落本地文件夹（一链接一文件夹：01.jpg…+meta.txt）+ 素材库该行 done + 图数；同链接（不同 token）重贴不重复建行；失败链接明确提示（xsec_token 过期…）
3. **定时队列**：加入队列 → 设置 → 定时任务 → 定时扒图「立即运行」→ 队列清空 + 新增记录进素材库
4. **链接详情**：图片网格（缩略图真实显示 + 宽高 + 水印 + 来源标记徽章「扒图」）+ 本地文件夹路径 + 「打开本地文件夹」按钮（实测弹出文件管理器）+ 「上传网盘」补传按钮 + netdisk 状态徽章/分享链接
5. **设置 → 扒图设置**：存储目录（+「打开存储目录」）/ 定时默认时间（保存同步定时页 cron）/ 上传默认开启 / 夸克登录块（授权码一次性输入，真实授权延后 manual）
6. **任务中心（/tasks）**：任务行标题可点 → **独立任务详情页**（完整内容全文/来源依据 evidence/流转历史/步骤/操作）
7. 扒图页**无 AI 产物按钮**（选品建议已撤——方向收口：素材库纯采集；AI 文案 SEO 初稿 / SKU·变体建档归 v0.7/v0.8 加工页）

## 三、集成验收端到端实测记录（2026-09-03 真跑，双服务 engine 8100 + web 8090）

- 本地 PG16 双库 alembic 迁移到 head（引擎 0002 / 业务 0011→0012）
- **立即扒**：贴链接 → 任务 DONE → link_record 落库（失败也有明确 error_note 页面可见）
- **定时队列（A62 实锤）**：加入队列 → 定时页「立即运行」→ 任务 DONE → **link_queue 清空**
- **定时默认时间（A63 实锤）**：扒图设置保存 08:30 → 引擎 schedule 行 cron 变 `30 8 * * *`
- **xhs 真实下载成功**：用户复核期间贴真实小红书分享链接 → 图落盘（365KB，真实 xsec_token 有效）——连接器真实可用
- **xhs vendor 子进程真跑**（未 mock）：无效 token → 未落盘 → ok=False +「xsec_token 过期，请重新取最新分享链接」明确提示
- **选品建议真实 LLM 端到端**：勾图 → 真 DeepSeek 生成建议 → 提案 pending（修复真实 LLM 输出规范化后）→ 批准成任务 → 任务详情页完整可见
- **集成真跑发现并修复的真 bug**：①调度器定时 input 模板缺 urls 键（定时链任务 failed「引用路径 'urls' 不存在」）→ 补 `urls: []` + 回归锁死；②缩略图路由 str 参数绑定 BIGINT 列 500（占位图根因）→ int 注解 + store 强制 int + 回归；③真实 LLM 自由格式输出被 TaskProposal 校验拒（选品建议看起来像占位）→ ACT 代码侧规范化 + 回归
- **manual 项**：真实分享链接冒烟已部分达成（用户提供链接成功下载）；闲鱼真实链接冒烟、夸克真实授权上传（用户拍板延后最后）、定时到点自动跑（种子已就位）待用户后续

## 四、复核反馈处理（2026-09-03 用户复核期，方向性澄清）

| # | 反馈 | 处理 |
|---|---|---|
| 1 | 图包是 zip 压缩包不行 → 一链接一文件夹（图片 + TXT 元数据，作者ID/网盘链接(如有)等） | zip 导出移除（A60 改语义）；下载归集一链接一文件夹 + meta.txt；默认存本地 + 可选上传网盘；多端访问靠网页预览 + 网盘（commit a3a6727/448cb1b/05949b3） |
| 2 | 扒图页「同步上传网盘」选项（默认勾选）+ 设置配夸克登录 | 复选框（默认勾）+ 下载完自动上传链接文件夹 + 永久分享链接回填 + 历史补传按钮 + 设置页夸克登录块；凭证形态随工具要求（第一版授权码，用户拍板延后真实授权）；上传逻辑 fake 测通（A70/A71/A72，commit ec42f2c） |
| 3 | 定时队列从设置移回扒图页 | 队列块全量迁移 /scrape（加入/查看/清空 + 定时时间提示）；设置页只留存储目录/定时时间/上传默认/夸克登录（commit 6be72e3） |
| 4 | 图片网格缩略图只有占位图 | 真 bug：缩略图路由 str 参数绑定 BIGINT → 500；int 注解 + store 强制 int + 回归测试（commit 2a88980） |
| 5 | 加「打开本地存储位置」按钮 | 链接详情「打开本地文件夹」+ 设置页「打开存储目录」；服务器文件管理器（xdg-open/open/explorer）+ 无桌面降级展示路径 + 越界防护（commit 2a88980） |
| 6 | 「生成选品建议」是否占位按钮 | 真功能但真实 LLM 自由格式输出被契约拒（提案进不了审核页）→ ACT 代码侧规范化（AI 提出、代码补全 domain/role/evidence 白名单化）+ 真实端到端验证提案落 pending（commit 3322141） |
| 7 | 提交后结果在哪看 | 提交后轮询 + 完成绿条「立即去审核 → /tasks」（commit cefd9ad） |
| 8 | 没有任务详情 | 独立任务详情页 GET /tasks/{id}（完整内容/来源依据/流转历史/步骤/操作，commit 41d2116，A75） |
| 9 | **方向性**：总体设计不对，不能直接到 AI 那里 | 澄清真实流程（图 → 修图[本质不变] → SEO → 上架）→ **扒图页撤 AI 产物按钮（纯采集管理）**；AI 文案（SEO 初稿）+ SKU/变体建档归**加工页**（v0.7 SKU 建档 / v0.8 卖法 SEO）；引擎 suggest 链保留资产无 UI（commit 3aff89a） |

## 五、交付规模

- 提交：v0.6 起 30+ 个（git log 实际为准；8 批开发 + 复核反馈处理）；全量单测 **885 passed 五绿全绿**，验收断言 **A1-A75 全绿**（planned 清零）
- 新增：素材库（scrape.link_record 一链接一条 + image_file 挂链接/来源标记 source_mark/SKU×店铺留位 + netdisk 三列，迁移 0011/0012）、下载链拆两链 + 归集一链接一文件夹 + meta.txt、连接器照广成搬回（xhs H1/H2 + 闲鱼 H3）、EngineContext.biz_client/storage_dir、quark connector + 上传执行点 + 补传链、定时队列回扒图页、独立任务详情页、检查器 A1-A75、B5 四断言真测试
- 已知打磨项：夸克真实授权与真跑上传（manual，用户拍板延后最后）；闲鱼真实链接冒烟（manual）；定时到点自动跑待部署验证；样式整体精修留待集中（决策 18）

## 复核结论

- [ ] 通过（待用户确认后 git tag v0.6 发版）
- [x] **通过（2026-09-03 用户确认「没啥问题了」+ 方向收口确认，git tag v0.6 发版）**
- [ ] 需修改：________________________________
