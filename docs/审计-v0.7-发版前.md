# 刘全 v0.7 发版前合规审计（2026-09-03 首版 + 2026-09-04 v0.7.1 修订复核）

> 依据：开发流程⑥「每版发版（git tag）前跑一次合规审计：文档与代码一致性、变更日志完整性、红线抽查」
> 审计时间：2026-09-03（用户首版复核前）→ **2026-09-04 修订复核**（用户首版 8 条问题修复后、待复核通过即 git tag v0.7）

## 〇、v0.7.1 修订复核（2026-09-04，用户 8 条问题修复后重审）

| 项 | 结果 |
|---|---|
| 红线复查 | ✅ 无新增风险路径：本轮新增仅 本地上传写盘（storage_dir 设置键 → selfshot 目录，upload_image makedirs）+ 建档/编辑表单 + 图库 JSON 读接口，无 Etsy 写、无 NocoBase 写、无外网调用 |
| commit 链 | ✅ 修订 5 commit 全部入库：790c97e（BUG-3 建档 500）→ 26b10dd（批 1 服务层）→ c23c2ef（批 1 验收补丁）→ fa0609e（批 2 页面层）→ 8183855（批 3 两 bug 修复 + 文档同步） |
| 五绿 | ✅ lint 0 违规 / registry 通过 / 全量单测 911 passed / verify 通过 / 断言覆盖通过（commit hook 逐次全绿） |
| 四方对账 A76-A95 | ✅ manifest ↔ test_acceptance_v07.py 21 条 ↔ 实质断言 ↔ 实现全一致（各批 B3 已核） |
| 整体独立测试（批 3） | ✅ 真实双服务 12 项走查 11/12 通过 + 抓 2 bug（gallery search datetime 500 / 详情页规格不渲染）修复闭环 + 回归锁补强（A92 详情规格徽章 / A94 datetime 序列化） |
| 复核走查自跑 | ✅ 9 项干跑全通过（真实表单：P-LTR-001/002 自动编号、C-GIFT-001 配方两行、编辑+详情规格渲染、上传→上传文件夹→文件名搜索 200）后数据清理归零（catalog 0） |
| dev 环境 | ✅ 业务库迁移 head=0014（product_code/specs 列）；双服务运行 v0.7.1 代码（web 8090 + engine 8100） |
| 文档同步 | ✅ 变更日志 / 项目状态 / CLAUDE.md / 复核报告（9 项走查表）已更新至 v0.7.1 |

## 一、红线抽查（安全红线零容忍，R13）

| 项 | 结果 |
|---|---|
| 密钥入 git（.env/credential/secret/token/.pem） | ✅ 无（git ls-files 与 git grep 双查为空） |
| 追踪文件硬编码密钥（sk-... 长串） | ✅ 无 |
| Etsy API 写操作（api.etsy.com/openapi.etsy 的 POST/PUT/DELETE） | ✅ 无（v0.7 引擎零改动、无平台写） |
| NocoBase 库写操作 | ✅ 无（v0.7 无 ERP 对接） |
| 本机直连 Etsy | ✅ 无（v0.7 无网络平台调用） |
| 密钥存储边界 | ✅ 密钥只进 .env（LIUQUAN_BIZ_API_TOKEN 等，gitignored） |

## 二、代码与文档一致性

| 项 | 结果 |
|---|---|
| v0.7 代码 commit 链完整（批1-6 + 修订批1-3 + bug 修复） | ✅ 全部入库（HEAD = 8183855） |
| 工作区无未提交代码改动 | ✅（仅审计文档本轮更新，随发版一起） |
| 详设 ↔ manifest ↔ 测试函数 ↔ 实现四方对账（A76-A95） | ✅ 全一致（各批 B3 已核） |
| 变更日志完整性 | ✅ v0.7 各批 + BUG 修复 + 修订批 1-3 均有留痕 |
| 测试数据清理 | ✅ catalog 全表 0 条（复核从干净状态开始） |
| 环境杂项 | ✅ .gitignore 补 dsh-readme-research/、cookies.txt |

## 三、验收状态

| 项 | 结果 |
|---|---|
| 五绿（lint/registry/单测/verify/断言覆盖） | ✅ 911 passed 无 skipped（修订后 commit hook 逐次全绿，主代理复跑） |
| v0.7 验收断言 A76-A95 | ✅ 21 passed（pytest tests/test_acceptance_v07.py） |
| 全版本断言 A1-A95 | ✅ 全绿（pytest -m version_acceptance，含 A87b BUG-3 回归锁） |
| manifest planned | ✅ 清零 |
| 整体独立测试 + 修复复测 | ✅ 批 3 真实双服务 12 项走查 11/12 + 2 bug（gallery search datetime 500 / 详情页规格不渲染）修复闭环 + 回归锁补强 |

## 四、审计结论

**红项：无。** v0.7（含 v0.7.1 复核修订）达到可发版状态，待用户复核（`docs/复核报告-v0.7.md` 走查 9 项）通过后 git tag v0.7。

## 五、审计发现并处理

1. `.gitignore` 补 `dsh-readme-research/`（环境下载目录非项目产物）+ `cookies.txt`（测试残留）
2. 变更日志批 6 重复条目去重（子代理自写条 vs 主代理验收条，保留后者）
3. 测试子代理污染业务库的 12 条 catalog 测试数据已清理（集成测试应连独立测试库——记录为后续改进点：v0.8 起集成走查用独立测试库或走查后清理）
4. 修订轮（2026-09-04）整体测试/干跑数据全部清理归零（catalog 0、验证图已删）
5. 修订轮新增上传播放路径仅落盘 settings 键指定目录（dev=/tmp/liuquan-scrape-test），生产须在设置页配置真实 storage_dir 后再用上传（记录为发版提醒）
