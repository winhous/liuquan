# 扒图与选品上架集成规划（v0.5 扒图重做）

> 状态：**待用户确认（2026-09-03 已拍板 5 项，规划更新中）** ｜ 依据：三工作台方案 v3（`/home/winhous/project/erp/etsy-orders/docs/新品上架三工作台方案_v3.md`）+ 用户 2026-09-03 拍板（废飞书入口 / 粘贴刘全立即·定时扒 / **产物进素材库：一个链接一条记录，本地或网盘** / ERP 管流程刘全供 AI / **SKU 单独规划** / 定时时间进设置 / 定价 v0.6 后 / 换背景 COMFYUI 以后做）
> 本规划回答：扒图要做什么内容、怎么做、以及和 SEO 优化 / AI 任务 / 图片处理 / ERP 上架 / SKU 怎么集成。

## 1. 用户真实业务全貌（三工作台 v3：选品→运营→采购）

```
选品岗（找货·洗图·建档）          运营岗（照单上架）            采购/生产岗（备货）
───────────────                   ──────────────              ───────────────
闲鱼/小红书看到货
   │
   ▼
【选品卡】（v3 在 ERP 建）
   来源链接/原图入库 → 系统体检（分辨率/水印）
   → Google 查重（人工，系统留记录）
   → AI 换背景定稿（ERP 侧：ChatGPT/Photoroom/通义万相）
   → 建档：全新=新建本地 SKU / 已有=绑货品主档（不裂物料）
     套装必配 BOM；变体 Style 绑本地 SKU
   → 标题/描述（DeepSeek 出初稿，人改）← 刘全 SEO 能力落点
   → Tag 池勾 13 个
   → 定价助手建议售价
   ▼
【派发上架】→ 上架任务单（ERP 左 + Etsy 右，一键复制）
   → 回填 Listing ID → 系统校验 → 平台在售档绑本地 SKU
   ▼
订单 → 平台 SKU → 本地 SKU（有图/库存/成本）→ 备货建议 → 采购单
```

**v3 三条铁律**：①物料 = 仓库里一件货，多店 = 多张在售档，不裂物料；②图从 ERP 带去 Etsy，新品不再扒回；③Etsy 变体 SKU 框填本地 SKU（订单自动对上的钥匙）。

## 2. 扒图在整条链里的定位（重做后的角色）

**扒图 = 素材采集 + 素材库（用户 2026-09-03 拍板）**：闲鱼/小红书链接 → 图片 + 商品元数据 → 落**素材库/图片库**（本地目录或网盘，**一个链接 = 一条记录**：图 + 元数据 + 状态）→ 后续供选品卡/SEO/上架引用。

- **不是**：网页贴链接下载完就完事（v0.5 现状，无去向，用户"几乎不能用"）
- **不是**：直接进 ERP 选品卡（选品卡衔接后置，先立素材库）
- **而是**：素材采集 → **素材库（链接记录 + 图 + 元数据）** → 选品建议/SEO 初稿（TM 提案）→（后续）选品卡/建档

### 素材库数据模型（重做详设定，方向）

- **链接记录**（一个链接一条）：url / 来源（xhs·xianyu·通用）/ 状态（pending·downloading·done·failed）/ 图片数 / 元数据（desc·tags·author）/ 落盘路径 / 批次 / 时间
- **图片**：挂链接记录（一个链接多张图：主图轮播/详情图），图级字段（宽高/水印/文件路径）
- **存储**：本地目录（scrape.storage_dir 设置键）优先；网盘（夸克）现状人工同步，后续评估自动（拍板 3：存储设置项可配）
- **产物导出**：按链接记录出「图包」（图 + 元数据清单），供选品卡人工上传或后续对接

## 3. 入口重做（用户拍板：废飞书入口）

| 项 | 设计 |
|---|---|
| 入口 | 刘全扒图页（保留改进 v0.5 页面，样式已 Tabler 化）：粘贴链接 → **立即扒** / **加入定时队列** |
| 立即扒 | 粘贴 → 提交 → 下载链立即跑（下载→体检→落素材库），结果回显素材库 + 可选触发选品建议进 TM 提案 |
| 定时扒 | 复用 v0.4 schedule 底座：链接队列存设置键（`scrape.link_queue`）+ **定时默认时间进设置**（拍板 3：设置项如 `scrape.schedule_time`），定时链批量扒 → 新增落素材库 → 结果进 TM 任务/飞书通知（可选） |
| 历史 | 链接记录列表（一个链接一条：状态/图数/元数据）+ 批次（batch_id）+ 状态（运行中/完成/失败） |
| 多链接 | 一次粘贴多行 = 批量建链接记录，逐条扒（一个链接一条记录语义） |

## 4. 扒图工序取舍（定稿：保留下游 + 搬回广成连接器细节 + 修通链路，不推倒）

**结论来源**：`docs/技术摸底-扒图工序对比-广成vs刘全.md`（子代理逐行对比，2026-09-03）。**总体判断：刘全 v0.5 扒图 = 广成「骨架化照搬 + 下游升级」，xhs 照搬是坏的、闲鱼兜底大幅缩水、业务链 6 断点；重做 = 保留下游设计、搬回广成连接器细节、修通链路，而非推倒重来。**

### 4.1 完全保留（刘全优于广成的部分）
下游设计（体检+选品→提案审核，方向正确）、ImagePack/SuggestionInput Pydantic 契约 + worker.yaml 声明式、http_image 通用图连接器 + xhslink/2.taobao 别名、页面多链接批量贴 + 徽章/缩略图/批量选品交互、reason:none 零 token 工序。

### 4.2 需补齐（照广成原样搬回）
- **xhs 两处硬伤**：①子进程调用 `from main import download` 必 ImportError（vendor main.py 无此函数）→ 改广成 `from source import XHS` + `XHS(work_path=..., folder_name='', folder_mode=True, image_download=True, video_download=False, record_data=True, download_record=False, image_format='JPEG')` + `extract(url, download=True)`；②元数据 SQL 用错表/列（note_data+英文列不存在）→ 改 `SELECT "作品描述","作品标签","作者ID" FROM explore_data WHERE "作品ID"=?`（recorder.py DATA_TABLE 实锤）
- **xhs 补齐**：差集计数（before/after 只数本次新增）、未落盘检查（paths 空 → ok=False +「xsec_token 过期，重新取最新分享链接」提示）、vendor 完整度检查（source/__init__.py）
- **闲鱼全套搬回**：11+6 套主图/详情选择器、`_harvest_all_imgs` naturalWidth≥100 尺寸过滤、9 类 URL 过滤关键词、`_DEAD_PAGE_KEYWORDS` 失效页删垃圾图、全局 90s deadline（非 set_default_timeout）、networkidle 优先降级 domcontentloaded、标题 page.title 去后缀+desc+h1 三路、卖家 window.__INITIAL_STATE__ 两路
- **元数据降级标注**：ok=True 时 note 记降级原因（db-missing/no-title/...），上层可区分真读到 vs 降级空

### 4.3 需改造（刘全自己的断点，见 §10）
链 input 契约统一（拆两链）、image_file 落库 + 补 A47/A48 验收、EngineContext 注入 biz_client（或 image_inspect 改走 provider）、product_suggestion 诚实化（真接 LLM 或改 reason:none）、定时触发落地。

> 视频/价格：两边都不支持/无先例，本规划不做（业务未排期）。

## 5. 与各系统集成设计（核心）

### 5.1 SEO 优化（标题/描述初稿 → v3 建档环节）
- 现状：seo_optimize 工序已四绿（titles 3-5/tags≤13/描述重写，playbook 外置）
- 集成：选品卡建档时「标题/描述」= **刘全 seo_optimize 出初稿**（输入：扒图元数据 + 可选图片 + 目标关键词）→ 提案进 TM → 人改后用于上架
- 落点：扒图页/选品卡上「生成 SEO 初稿」按钮 → seo_optimize_chain → 结果回显（v0.5 已建 seo_optimize_chain + 提案流）

### 5.2 AI 任务（TM 提案审核流，决策 12/16 延续）
| 触发 | 提案/任务 | domain |
|---|---|---|
| 扒图完成 | 图片体检异常（分辨率不足/水印）→ 建议换图 | scrape |
| 扒图完成 | 选品建议（product_suggestion，元数据+AI） | scrape |
| 体检发现下滑 | 优化建议（seo_healthcheck 已建） | seo |
| 建档请求 | 标题/描述初稿（seo_optimize） | seo |
| 上架请求 | 上架任务单（人工确认后进 TM，运营执行） | seo/scrape |

### 5.3 图片处理
| 处理 | 归属 | 状态 |
|---|---|---|
| 图片体检（分辨率/水印） | 刘全 image_inspect（已建） | ✅ v0.5 |
| 图库（scrape.image_file 元数据） | 刘全（已建） | ✅ v0.5 |
| 缩略图展示 | 刘全（/scrape/thumbnail 已建） | ✅ v0.5 |
| AI 换背景/去水印/生成图 | **ERP 侧**（v3：ChatGPT/Photoroom/通义万相，玻璃材质注意变形） | 刘全不做（边界，后续可评估） |
| Google 查重 | **人工**（v3：ERP 给按钮，阿里云出口不适合撞 Google） | 刘全给「打开搜图」入口记录 |

### 5.4 ERP 上架（三工作台流程在 ERP，刘全 AI 供数）
- 刘全**不建选品工作台 UI、不建 SKU 主档、不写 Etsy**（红线：一期不写 Etsy；ERP 流程归 ERP）
- **SKU 建档单独规划（用户 2026-09-03 拍板：SKU 是一个大功能，本规划不含）**
- 刘全对 ERP（后续）：素材库链接记录/图包 → 选品卡（衔接方式后置拍板）；SEO 初稿/选品建议 → 选品卡字段；上架任务单（刘全 TM 任务 = 人工执行入口）
- ERP 对刘全：NocoBase 只读 v_* 视图（v0.6 才接）——本阶段素材库独立于 ERP，选品卡衔接优先「人工图包」而非直写 ERP（红线：刘全一期不写 NocoBase，决策 6 ERP 只读）

### 5.5 SKU（单独规划，用户拍板）
- **SKU = 单独大功能，另行规划**（建档规则/编码体系/变体绑定/老货迁移，结合 v3 方案与 ERP 现状）
- 本规划只留位：素材库/选品卡 → 建档时 SKU 规则由后续 SKU 规划定义

## 6. 刘全 vs ERP 职责边界（用户拍板：ERP 管流程，刘全供 AI）

| 环节 | 归属 | 说明 |
|---|---|---|
| 素材采集（扒图） | **刘全** | 粘贴链接 → 立即/定时扒 → 图库 |
| 图片体检 | **刘全** | image_inspect |
| 选品建议 | **刘全** | product_suggestion → TM 提案 |
| 标题/描述/Tag 初稿 | **刘全** | seo_optimize → TM 提案 |
| 定价建议 | **刘全可选** | 数据来自 ERP 视图（v0.6 接）后做 |
| 选品卡/查重/换背景/建档/SKU/BOM/Tag 池 | **ERP** | 三工作台 v3 |
| 上架任务单/回填 ID/在售档 | **ERP** | 三工作台 v3 |
| 订单/库存/成本/采购 | **ERP** | 只读视图接刘全（v0.6） |
| Etsy 写操作 | 无（人工 + 任务单） | 红线 |

## 7. 待拍板点（2026-09-03 已拍板，剩余开放项）

**已拍板**：
- A. 产物去向：**素材库（一个链接一条记录，本地或网盘）**——不直接进 ERP 选品卡；选品卡衔接后置（素材库 → 图包人工/后续对接）
- B. SKU：**单独规划（大功能）**，本规划不含
- C. 定时扒：**默认时间进设置**（设置键 `scrape.schedule_time`）
- D. 定价建议：**v0.6 接 ERP 数据后再做**
- E. 换背景/图片生成：**外接 COMFYUI，以后做**（大功能）

**仍开放（重做详设前定）**：
- 素材库网盘自动同步要不要做（夸克个人版无开放 API，现状人工同步；自动需第三方挂载方案）——倾向本阶段本地目录 + 人工同步
- 素材库要不要带「选品建议/SEO 初稿」按钮（每链接记录可触发，进 TM 提案）——倾向要（§5.1/5.2 已设计，重做阶段 3）

## 8. 分阶段实施（用户确认规划后细化）

- **阶段 1 入口与队列**：扒图页改「立即/定时」+ 链接队列设置键（`scrape.link_queue`）+ 定时默认时间设置键（`scrape.schedule_time`）+ 定时扒链（复用 schedule 底座）——v0.5 补丁
- **阶段 2 素材库**：链接记录表（一个链接一条：url/来源/状态/图数/元数据/落盘）+ 图片挂链接 + 图包导出（供选品卡人工衔接）；补 xhs/闲鱼连接器（照广成搬回，H1-H3）与链落库（T1-T8）
- **阶段 3 SEO 建档联动**：素材库链接记录「生成 SEO 初稿」按钮 → seo_optimize_chain → 提案 → 回显
- **阶段 4 补强（单独规划/后置）**：SKU 建档（单独大规划）/ 定价建议（v0.6 接 ERP 数据）/ 换背景图片生成（外接 COMFYUI）/ 网盘自动同步（评估第三方挂载）

## 10. 技术整改项（v0.5 扒图实现断点，重做范围——2026-09-03 排查 + 对比报告确认）

### 10.1 连接器硬伤（对比报告 §2 实锤，重做必改）

| # | 硬伤 | 现状 | 重做方案 |
|---|---|---|---|
| H1 | **xhs 子进程调用必失败** | `from main import download`（vendor main.py 无此函数 → ImportError） | 改广成 `from source import XHS` + `extract(url, download=True)` |
| H2 | **xhs 元数据必空** | SQL 查 `note_data` 表 + 英文列（库实际落 `explore_data` 表 + 中文列） | 改 `SELECT "作品描述","作品标签","作者ID" FROM explore_data WHERE "作品ID"=?` |
| H3 | **闲鱼兜底缩水** | 自创 3+3 简化选择器，无尺寸/URL 过滤、无失效页删图、90s 语义错 | 照广成搬回 11+6 选择器 + naturalWidth + 9 类关键词 + 失效页 + 全局 deadline |

### 10.2 链路断点（对比报告 §4.2 + 主代理实测）

| # | 断点 | 现状 | 重做方案 |
|---|---|---|---|
| T1 | 链 input 契约不匹配 | scrape_suggest_chain 输入 = SuggestionInput{image_ids}，贴链接传 urls → 引擎 422（已实测） | **拆两链**：scrape_download_chain（贴链接：urls → 下载+体检 → 落图库）+ scrape_suggest_chain（勾图：image_ids → 建议 → 提案）；输入模型各自明确 |
| T2 | image_download 步骤缺 url/batch_id | 链步骤无 input 表达式 → 直传 task_input {urls, source}，单/多链接无人拆分 | 下载链输入 ScrapeChainInput{urls[], batch_id}，步骤 input 引用拆分 |
| T3 | image_file 无落库调用方 | 全仓无代码调 POST /api/biz/scrape/images（api_biz 只定义端点）；A47 验收测试不存在 | 下载链内加落库步骤（幂等 409），链完成即图库可见；**补 A47/A48 真验收** |
| T4 | EngineContext 无 biz_client | image_inspect 的 ctx.biz_client 恒 None → 体检不可用 | EngineContext 注入 biz_client（决策 26 写接口化）或 image_inspect 改经 provider 读 local_path |
| T5 | product_suggestion 假 LLM | worker.yaml reason:llm 但 run.py 纯模板拼装（无 LLM 调用） | 诚实化：真接 LLM（prompt 已有模板，元数据入 prompt）或改 reason:none（二选一，按 §5.2 集成定） |
| T6 | 页面 JS 端点 | 已改 /scrape/run + /scrape/suggest（临时） | 重做时并入正式设计（拆两链后的提交接口） |
| T7 | 立即/定时 | 仅手动 | 立即 + schedule 定时链（链接队列键 `scrape.link_queue`） |
| T8 | 产物去向 | 图库与选品卡无衔接；用户拍板改**素材库**（一个链接一条记录，本地/网盘） | 重做：链接记录表 + 图片挂链接 + 图包导出；选品卡衔接后置 |

> 说明：硬伤与断点均已实证（对比报告行级证据 + 主代理引擎 422 实测 + 全仓 grep 无落库调用方）；**A47/A48 验收断言此前"通过"为假绿**（test_acceptance_v05.py 无 test_a47，链未跑通时断言缺失）——重做阶段 1 补真验收，如实记录（变更日志 2026-09-03）。

---

## 9. 不变量（红线）

- 不写 Etsy（上架人工 + 任务单）；不写 NocoBase（只读 v_* 视图，v0.6 接）；密钥只进 .env；AI 只提建议人拍板（提案审核）；详设先行再动代码

---

## §A 附录：广成 vs 刘全扒图工序对比（子代理报告，2026-09-03 完成）

**结论**：刘全 v0.5 扒图 = 广成「骨架化照搬 + 下游升级」，xhs 照搬是坏的（调用必 ImportError + 元数据 SQL 表/列名全错）、闲鱼兜底大幅缩水（11+6 套选择器 → 自创 3+3，无过滤/失效页/全局截止）、业务链 6 断点（链 input 契约、image_file 落库、biz_client 注入、product_suggestion 假 LLM、定时、JS 端点）。重做 = **保留下游设计 + 搬回广成连接器细节 + 修通链路**，不推倒。

- 广成下游唯一消费者 = feishu-write（扒图产物 → 飞书多维表格人工记录，无 AI 加工）
- 刘全下游 = 体检 + 选品提案 → TM 审核（更完整的产品闭环，方向正确但未跑通）
- 视频两边都不支持（广成显式 video_download=False）；价格两边都没有（闲鱼未扒价格，需新增则无广成先例可抄）

完整对比表见 `docs/技术摸底-扒图工序对比-广成vs刘全.md`（含行级证据：vendor main.py 无 download 函数、recorder.py DATA_TABLE 落库结构实锤、闲鱼选择器列表等）。
