# 刘全项目状态

> 本文件只描述**当前最新状态**，与项目实际保持完全一致，每次状态变化即更新。
> 历史与过程看 `变更日志.md`；计划看 `版本规划.md` 与 `建设方案.md`。

**更新日期：2026-08-30（v0.1 已发版 git tag v0.1；v0.2 已发版 git tag v0.2；v0.3 已发版 git tag v0.3，用户复核通过，2026-08-30）**

## 当前阶段

**v0.3 CRM 平移已发版（git tag v0.3，2026-08-30 用户复核通过）** --需求对齐拍板决策 19-32，详设 `docs/详设-v0.3-CRM.md` 确认。三批开发完成（批 1 架构与工序：crm 迁移+ORM/api_biz 读写接口/五工序三链两 provider/消费者接口化（决策 26 引擎零业务库连接串）；批 2 CRM 页面：活档案列表/详情/粘贴异步链/回复台/确认事务/翻译工具/客户删除/飞书；批 3 标签与流转：next_action/自然语言入口（tm_intent）/下一步区）。**集成验收真跑**：双服务端到端（粘贴→真 DeepSeek 三工序 4.3s<6s→译文/快照落库→候选经写接口落库→确认→tm.task crm/ai/open）+ 收尾修复（todo_generate prompt 简化+evidence fallback/白名单 int id/steps_output/0004 迁移）。**用户复核**（2026-08-30）：复核反馈 #1-#6 全部处理（#1/#4 任务清单固定高度 620px+滚动 / #2 完成作废去编辑按钮 / #3 操作按钮统一 btn-sm / #5 派生 422 bug 修复 / #6 任务步骤轻量子任务（task_step 迁移 0005 + 完成守卫 + N/M 徽章 + 步骤弹窗）+ 派生弱化为快速建关联任务（link_parent 默认关）+ 逾期筛选布局修复（筛选器 flex:0 0 auto 防吞剩余空间，表格贴顶））。**全量 575 passed 四绿全绿，验收断言 A1-A37 全绿（`pytest -m version_acceptance` = 41 passed）。复核报告 `docs/复核报告-v0.3.md` 结论：通过。git tag v0.3 发版。**

**v0.2 任务进度（8 项全部完成）：** 任务 1 业务库迁移+ORM ✅（migrations/business + models/tm.py）→ 任务 2 engineapi 客户端 ✅（web/engineapi，三接口 HTTP 客户端；trust_env=False 修复系统代理坑）→ 任务 3 TM 转交器 ✅（engine/actions/tm_proposal.py，禁幻觉三件套 + 幂等/防重）→ 任务 4 demo_propose 工序 ✅（纯代码 reason:none + tm_demo_chain 演示链）→ 任务 5 引擎 server 三接口 ✅（engine/server.py：FastAPI 三接口 + 后台队列消费循环（并发上限 2）+ TM 转交器钩子 + 崩溃恢复；engine/core/db.py 增 claim_task/get_last_step/list_tasks_by_status）→ 任务 6 web 接真数据 ✅（web/tm_store.py 独立 DAO + web/app.py async 路由 + tasks.html 全要素）→ 任务 7 测试 ✅（fixtures/tm 假样本 + fake_providers + test_antihallucination + test_acceptance A12-A26，472 单测）→ 任务 8 集成验收 ✅（端到端真跑 + systemd 单元 scripts/deploy/）。

**v0.1 全部完成（含 A11 真模型冒烟）** --详设 §11 文件清单全部落地（T3-T12 + T11），四绿总门**全真跑无 skipped**，验收断言 **A1-A11 全绿**（A11：翻译雏形真调 DeepSeek 成功——审计真值落库、成本 ¥0.0006 < 0.01 元），全量单测 339 通过 + 代码审查修复批次（C1 等 8 个真问题修复 + 6 条回归测试）。**2026-08-28 用户复核通过，git tag v0.1 发版。**

```text
需求分析 ✅ -> 概设 ✅ -> 详设 ✅（v0.1/v0.2/v0.3）-> 页面原型 ✅ -> 开发 ✅（v0.1/v0.2/v0.3）
-> 测试 ✅（A1-A11 + A12-A26 + A27-A37）-> 集成验收 ✅ -> 用户复核 ✅（v0.1/v0.2/v0.3 tag）-> v0.4 启动
```

## 仓库现状

```text
liuquan/
├── CLAUDE.md              项目说明（架构/红线/技术栈）
├── pyproject.toml         uv 项目定义（console_scripts: liuquan-engine；pydantic-ai/sqlalchemy/asyncpg/dotenv/pyyaml）
├── uv.lock                uv 依赖锁文件
├── models.yaml            模型注册（§7.1：default 别名，env: 前缀，换模型只改这里）
├── .env.example           密钥变量名样例（R20：只留名不留值）；本机 .env 为占位（gitignored）
├── .gitignore
├── engine/                引擎包
│   ├── cli.py             五命令 CLI（registry-check/run/resume/audit/verify，退出码 0/1/2）
│   ├── server.py          常驻 server（v0.2 T5）：FastAPI 三接口 + 后台队列消费循环（并发上限 2）+ TM 转交器钩子 + 崩溃恢复
│   ├── actions/           Action 消费者注册表 + TM 转交器（v0.2 T3）+ CRM 候选消费者（v0.3，crm_candidate）+ biz_client（决策 26 引擎零业务库连接串）
│   ├── providers/         Context provider（v0.3：crm/tm 域数据供给，白名单来源）
│   ├── core/
│   │   ├── statemachine.py  §2.2 相位转换表写死 + 转换守卫（T5）
│   │   ├── policy.py        §5.2 域权限矩阵 + 风险门禁（T5）
│   │   ├── context.py       EngineContext（P3-1 工序签名 ctx 类型，含 llm_output）
│   │   ├── checkpoint.py    检查点读写 + 恢复定位（T9）
│   │   ├── audit.py         审计三态记录 + 脱敏摘要 + DbAuditGate（T9）
│   │   ├── runner.py        执行链：SKIP LOCKED 队列 + 五相位循环 + 崩溃恢复（T12a）
│   │   ├── db.py            引擎库 DAO（13+1 函数，ORM 与迁移同源；get_step 为 OBSERVE 扩展）
│   │   └── llm/             PydanticAI 封装：models.yaml 加载 + agent_factory 注入 + 三级重试（T8，P2 豁免区）
│   ├── registry/
│   │   ├── schema.py        五种 YAML 的 Pydantic schema（T4b）
│   │   ├── loader.py        L1-L9+L10 校验 + validate/load_registry/write_hashes（T4b）
│   │   ├── model_hashes.yaml  L9 Model 结构摘要登记（--write-hashes 生成，4 条）
│   │   ├── workers/         v0.1 demo 两工序 + v0.3 crm 四工序（chat_translate/snapshot_update/todo_generate/customer_reply_draft）+ tm_intent（意图理解）
│   │   ├── chains/          demo 链 + v0.3 三链（crm_chat/crm_reply/tm_intent）
│   │   ├── context/         demo/crm/tm 事件·Context·Action 登记位
│   │   ├── events/          events/demo.yaml
│   │   └── actions/         actions/demo.yaml
│   └── lint/               P1/P2/P3 机器执法（T2+T11；P1 词表自动生长，扫描对象内）
├── models/
│   ├── contract/           四契约冻结（Event/Action/Task/Context + 业务校验，T4a）
│   ├── crm.py              v0.3 crm schema 四表（customer/message/snapshot/todo_candidate + suggested_next）
│   ├── tm.py               v0.2 tm 三表 + v0.3 扩展（tags/ai_suggestion/detail）+ v0.3 task_step
│   └── workers/            工序 input/output Model（7 个，T10）
├── migrations/engine/      Alembic：4 表 DDL 逐列对齐 §3（T6；async 模式）
├── migrations/business/    Alembic：业务库 5 个迁移（0001 tm 基础 → 0002 crm 四表 → 0003 tm 扩展 → 0004 suggested_next → 0005 task_step）
├── web/                    v0.2/v0.3 业务应用（FastAPI + Jinja2 + Tabler 1.4）
│   ├── app.py              create_app 工厂（MODULES 导航 + /tasks 真实数据 + /crm* + /translate* + 状态操作/派生/编辑/重开/人工建任务/提案审核/触发演示链/步骤/下一步；未登录重定向 /login）
│   ├── tm_store.py         web 侧任务 DAO（models/tm.py 共享 ORM + SQLAlchemy async，连接串读 .env 的 LIUQUAN_TM_DB_URL，零 engine import）
│   ├── crm_store.py        v0.3 CRM DAO（四表读写 + 确认事务 + 删除级联 + 翻译工具 + 回复台）
│   ├── api_biz.py          v0.3 业务写入接口（/api/biz/*，决策 26：引擎经 HTTP 写入的唯一通道，X-Biz-Token 校验）
│   ├── feishu.py           v0.3 飞书 webhook 卡片（决策 21，未配置静默）
│   ├── engineapi/          引擎三接口 HTTP 客户端（v0.2 T2：create_task/get_task/list_registry）
│   ├── templates/          base（Tabler treeview 两级导航）/ login / tasks / crm（index/detail）/ translate / module
│   └── static/             app.css + vendor/tabler（vendored，大陆可用，零 CDN）
├── tests/                  575 条单测（fake LLM 零网络 + 嵌入式 PG 真 SQL + @version_acceptance A1-A37）
│   ├── conftest.py         嵌入式 PG16 fixture（tests/.pgdata/，opt-in）
│   └── test_acceptance.py  A1-A37 验收断言
├── scripts/
│   ├── check.sh            四绿总门（v0.1 全真跑，无 skipped）
│   ├── pgdev.sh            本地 PG16 集群管理（T3 无 root 部署）
│   └── hooks/pre-commit    git pre-commit hook（core.hooksPath 指向本目录）
└── docs/
    ├── README.md           文档索引
    ├── 概要设计-v0.3.md    业务拍板基线（9 章）
    ├── 建设方案.md         各版本详细实施内容
    ├── 详设-v0.1-引擎地基.md  v0.1 详设（定稿 2026-08-27，v0.1 开发唯一依据）
    ├── 详设-v0.2-TM.md     v0.2 详设（2026-08-28 定稿确认，v0.2 开发唯一依据）
    ├── 详设-v0.3-CRM.md    v0.3 详设（2026-08-28 确认，v0.3 开发唯一依据）
    ├── 复核报告-v0.2.md    v0.2 复核报告（✅ 通过，git tag v0.2）
    ├── 复核报告-v0.3.md    v0.3 复核报告（✅ 通过，git tag v0.3）
    ├── 开发规范.md         25 条可判定规矩（R1-R25）
    ├── 开发流程.md         开发强制机制（三层防线六项设置）
    ├── 方法论报告-AI可控与反幻觉.md  AI 守规范+反幻觉方法论与两项目遵守审计
    ├── 版本规划.md         版本里程碑 v0.1~v1.0
    ├── 变更日志.md         改动流水账
    └── 概设-v0.2-存档.md   早期草稿
```

git：97 个提交（以 `git log` 实际为准），tags：v0.1 / v0.2 / v0.3。

## 版本进度

| 版本 | 主题 | 状态 |
|---|---|---|
| v0.1 | 引擎地基 | **✅ 已发版（git tag v0.1）**：四绿全真跑 + A1-A11 全绿（含真模型冒烟），2026-08-28 用户复核通过 |
| v0.2 | TM + 引擎接通 | **✅ 已发版（git tag v0.2）**：T1-T8 全部落地 + 集成验收通过 + A12-A26 全绿 + 复核期样式反馈修复，2026-08-28 用户复核通过 |
| v0.3 | CRM 平移 | **✅ 已发版（git tag v0.3）**：三批开发 + 集成验收通过（575 passed 四绿全绿，A1-A37 全绿）+ 复核反馈 #1-#6 全部处理（含任务步骤/派生弱化/逾期布局修复），2026-08-30 用户复核通过 |
| v0.4 | 调度完整版 | 未启动（用户已确认进入 v0.4，2026-08-30） |
| v0.5 | SEO + 扒图 | 未启动 |
| v0.6 | ERP 增强 | 未启动 |
| v1.0 | 一期发版 | 未启动 |

## 已拍板决策（36 项，2026-08-26 起）

1. 新建刘全 AI 引擎（取广成之骨 + PydanticAI 之心，全新代码库）
2. 分层统筹：工序为体、调度为纲，不做自主 Agent
3. 一期范围：TM / CRM / ERP / SEO / AI 引擎 / 扒图
4. PG16 双 database（业务库 + 引擎库）
5. 部署阿里云（与 NocoBase 同机），Etsy 数据链路不动
6. ERP 一期只读增强，重构挂起
7. EOMS 退役（v1.0），广成退出生产链路
8. 技术栈全免费开源，唯一成本 DeepSeek API
9. 权限从简（共享密码 + 三角色）
10. AI 100% 可控为最高工程原则

**补充拍板（2026-08-27）：**

11. TM 细则（2026-08-28 修订：**取消 P0-P3 优先级分级**——无分级标准、AI 分级不准确、人工难界定 P0/P1 区别；逾期标红保留、「逾期自动升级」随分级取消；后续有标准则 AI 按标准分级再议）/ 按角色指派 / 截止手填 + AI 建议 / 五态 + blocked / 循环任务交定时链
12. 全部 AI 提案人工审，不设代码自动通过
13. SEO/流量数据来源用 eHunt，流量维度一期不做
14. AI 统筹深度确认：预定义链即可，不做跨域每日重点（原文「+ 任务优先级」随决策 11 修订取消分级而失效）
15. **统计数据一定会做（2026-08-28 用户确认）**：工作台菜单当前版本无内容（统计内容后续在定）；方向确认后按需纳入后续版本/提级评估
16. **任务生成细则（2026-08-28 用户确认）**：任务生成双通道（AI 生成 + 人工输入）；任务来源 = 业务系统（CRM/ERP/SEO 等）+ 任务系统自身，task.domain 标来源域；AI 提案审核依据强制展示（每条 evidence 类型+ref_id+原文摘录+查看入口），AI 可引用数据受链白名单封闭，**100% 禁止幻觉**（机器校验拒落）
17. **任务生命周期细则（2026-08-28 用户确认）**：两种结束方式（完成/作废）**都必须人工回复，不回复不能结束**；派生新任务不代表原任务结束；权限 v0.2 不做、**将来必须引入完善权限系统（精确到人和数据，同页面不同人看到不同任务数量与内容）**，架构留位；同一件事不生成同样任务（同业务对象+同建议类型防重），未完成任务一直挂着不自动清理；任务生成后有权限人员可编辑；逾期一直提醒 + 列表筛选（筛选条件后定，一定有）
18. **样式打磨延后（2026-08-28 用户拍板）**：样式类问题（颜色/边框/字体等）不阻塞开发，v0.2 功能验收优先，样式打磨集中后调（见「样式打磨待办」段）

**补充拍板（2026-08-28，v0.3 需求对齐）：**

19. **CRM 待办衔接**：AI 待办候选落 `crm.todo_candidate`（带 evidence 依据）→ 客户详情页确认（=人工审核动作，依据强制展示，延续决策 12/16）→ 直接落 `tm.task`（domain=crm, source_type=ai），待办出现在 TM 清单标来源 CRM 徽章；**不设第二套轻量待办状态机**（调研 Asana/Jira+Salesforce：一套任务体系，客户档案 = 任务过滤视图）
20. **EOMS 存量数据不迁移**：「老版刘全的数据不要了」——v0.3 不做迁移脚本，从零录数据；验收取消「存量迁入对账」
21. **飞书通知 v0.3 做**：飞书群自定义机器人 webhook 卡片；`.env` 配 `LIUQUAN_FEISHU_WEBHOOK_URL`；触发点 = CRM 待办确认生成 TM 任务时推任务卡片；未配置不阻塞
22. **EOMS 功能取舍**：客户删除平移保留（级联清理+二次确认）；店铺级待办不做（TM 任务中心等价覆盖）；跟进候选并入 todo-generate 产出（决策 11 取消分级后 priority 失效）
23. **独立翻译工具 + 历史记录入口（2026-08-28）**：①CRM 导航「对话翻译」子页 = 独立翻译工具——粘贴任意文本→选语言→翻译→译文可复制，不落客户档案，可选「归入某客户」；②客户详情「粘贴对话」即历史记录入口——历史对话与正常对话同一入口正常走链（翻译+快照+待办），AI 理解上下文滚动快照、后续新对话以其为上下文，message_time 不标
24. **任务只按 domain 区分（2026-08-28）**：任务不分「客户级/店铺级」，一律按来源业务系统（domain：crm/erp/seo/tm）区分（列表来源徽章）；tm.task 不加 customer_id 列（保持通用）；客户 = CRM 域任务的关联对象——CRM 任务落库时 source 记 customer_id（关联+追溯），客户详情页任务卡 = domain=crm 且 source.customer_id=X 的过滤视图
25. **任务标签机制（2026-08-28）**：tm.task 加 tags（JSONB 数组，开放标签，起步约定 采购/物流/报价/售后/定制/发货）；AI 生成任务时建议标签（todo-generate 候选带 suggested_tags），人工可改可删；标签 = 将来跨系统流转（人工触发+权限控制）的判断依据
26. **业务写入接口化（2026-08-28）**：业务系统（tm/crm/erp）暴露写入接口——不区分 AI 写/人写，只校验数据正确性，正确即入库；AI/引擎（含代码消费者）禁止直连业务库，只能经接口写入；v0.3：crm 按接口化新建、tm 转交器改 HTTP 调 web 写入接口
27. **任务流转机制（2026-08-28）**：AI 建议下一步（默认值）+ 人同意/改派/忽略（决定权在人）；人选择与 AI 建议不同时 AI 提示异议但不阻止；分歧留痕（task_event 记 AI 建议 vs 人选择）；流转实体随版本接入（v0.6 ERP/v0.5 SEO）；精确到人待权限系统
28. **流转自然语言入口（2026-08-28）**：任务「下一步」区支持自然语言指令（如「流转到 ERP 库存」）——AI 解析为结构化指令 → 人确认 → 代码执行；解析模糊提示澄清不猜测；目标域未接入明确提示不可执行并记录意图
29. **对话图片全归 v0.5（2026-08-30）**：v0.3 不做图片功能（粘贴对话只处理文本）；CRM 对话图片链接记录→下载→存储→展示→OCR 规划到 v0.5 扒图域
30. **设置与权限独立版本 v0.7（2026-08-30）**：基础设置（店铺/AI/通知/系统参数）+ 权限系统（用户/角色/行级权限，决策 17 承诺）集中为 v0.7（v0.6 后、v1.0 前）；v0.3 不加设置菜单
31. **任务步骤模型 = 轻量子任务（2026-08-30）**：任务可拆步骤（内容+勾选框，无负责人/日期/工时）；父任务完成依赖全部步骤勾完（有未勾步骤 → 完成被拒）；步骤不派生新任务（v0.3 不做「步骤→子任务」自动映射，步骤即完成检查单）；派生功能弱化为「快速建关联任务」——页面默认不勾选关联父任务（link_parent 默认关），勾选才建父子链
32. **web 层步骤删除允许（2026-08-30）**：task_step 删除走 POST /tasks/{id}/steps/{sid}/delete（DELETE 语义，浏览器表单兼容）；步骤删除 = 人工维护操作，不属于 A25「静态检查不得提供删除」的 AI 内容删除范畴（delete_step/delete_customer 并列豁免）
33. **系统收敛 + 设置页清单（2026-08-30）**：用户评估「系统太大，按当前方式开发出来大概率不能用」→ v0.4 收敛为**定时闭环小版本**（定时触发底座 + 导航「设置 → 定时任务」页 + 首批定时链 CRM 未跟进提醒；SEO/扒图/ERP/权限全部延后不排期）。**导航新增「设置」，v0.4 只做定时任务管理一项，其余设置内容往后放**。设置页完整清单（settings 表键值对 + 页面，决策 30 范围内）：①店铺管理（名称/备注/状态，CRM source_shop 候选）；②AI 配置（API 密钥页面化、模型选择、风格指南/术语表）；③定时任务模块（定时链清单：几点跑/开关/上次下次运行，v0.4 先做）；④通知配置（飞书 webhook 页面化，决策 21 留账）；⑤权限系统（用户/角色/行级权限/登录改造，v0.6）；⑥系统参数（逾期天数等去硬编码——现 `FOLLOW_UP_DAYS = 5` 硬编码在 crm_store.py）。**settings 表 = 配置地基**：读配置先查表、表无回退默认（文件/.env），老配置不删平滑迁移，不存在返工
34. **设置提前到 v0.4 全量做（2026-08-30 修订决策 33）**：用户「现在可以做设置了，除了权限，别的都写到设置里」——目的：**先立配置地基，后续开发自然想到参数去设置里读，杜绝硬编码继续滋生**。v0.4 范围扩为「定时闭环 + 设置页五项」：①店铺管理（新建店铺表 + 管理页，CRM source_shop 候选）；②AI 配置（模型选择页面化、风格指南/术语表编辑、API 密钥填写入口）；③定时任务模块（原计划）；④通知配置（飞书 webhook 页面化）；⑤系统参数（跟进天数/逾期天数等现有硬编码全量搬 settings 表）。**密钥存储边界：DeepSeek key 等敏感密钥底层仍存 .env（R20 密钥不进数据库），设置页是"写入 .env"的友好壳**。权限系统（用户/角色/行级/登录改造）仍留 v0.7（重头戏单独成版）；v0.7 主题由「设置与权限」改为「权限系统」
35. **设置补充三项（2026-08-30，基于代码摸底）**：v0.4 设置范围在决策 34 五项上再补：⑥**登录密码**（共享密码存 settings 表（哈希）+ 登录真校验 + 设置页改密——补当前"表单有密码框但后端不校验、填什么都放行"的门禁漏洞，现状 = 系统裸奔；v0.7 权限系统做多用户+行级权限，v0.4 先补共享密码真校验，不冲突）；⑦**AI 引擎参数**（engine/core/runner.py 硬编码 `_DEFAULT_MAX_ATTEMPTS=2`/`_DEFAULT_TIMEOUT_S=30`/退避上限 30s 改读 settings，模型配置页可调）；⑧**列表分页/定时默认时间**（客户列表 page_size=20 等写死值、定时链默认跑的时间作为系统参数键）
36. **设置导航与独立提交机制（2026-08-30 修订决策 35，2026-08-30 再修订导航形态）**：用户拍板设置页交互形态——**⑥登录密码、角色 v0.4 先不要**（撤销决策 35 之⑥，随 v0.7 权限系统；门禁暂维持现状）。**导航**：一级「设置」下每个设置项 = 独立二级菜单；**AI 设置 = 分组标题（不可点，纯分组）**，其下 API 密钥/模型选择/风格指南术语表是**真正的叶子菜单**（用户评估后拍板：不用 TAB 页——"AI 设置"四个字只做分组，下面的 API 设置才是真二级菜单；Tabler nav-group 实现，成本与三级导航相同），**v0.4 只做 API 密钥，其余叶子占位后续慢慢加**。**独立提交机制（适用于所有设置）**：每个设置块 = 独立小表单 + 独立提交按钮 + 独立接口，提交后 HTMX 局部刷新该块（不整页、不牵连其他块）；settings 表按 key 存，每次提交只更新本块涉及的 key（原子更新不动其他 key）；店铺每条记录独立存、定时每条链独立存、系统参数每个键独立存——改 API 只提交 API，避免"一次提交全量设置"不合理

## 开发强制机制

`开发流程.md`（2026-08-26 建立）：三层防线六项设置，确保 AI 开发按文档目标与规范执行。

- 已在位：CLAUDE.md 铁律 7 条（入口卡）、详设先行流程约定
- **四绿全真（v0.1 落地完毕）**：绿 1 lint（P1/P2/P3 全规则 0 违规）、绿 2 registry-check（真跑）、绿 3 单测（真跑）、绿 4 verify（真跑），`scripts/check.sh` 无 skipped 分支命中，pre-commit hook 强制
- **验收断言**：A1-A11 全绿（`pytest -m version_acceptance` 跑 A1-A10；A11 真模型冒烟 2026-08-28 已执行：真调 DeepSeek 成功、审计真值落库、成本 ¥0.0006）；**A12-A26 全绿（v0.2，`pytest -m version_acceptance` 26 条，2026-08-28）**；**A27-A37 全绿（v0.3，`pytest -m version_acceptance` = 41 passed，2026-08-30）**
- 每版发版前跑合规审计（v0.2 复核后、git tag 前）

## 下一步

1. **v0.4 定时闭环 + 设置页（配置地基）**（决策 33/34/35/36，2026-08-30 用户拍板）——先立配置地基（settings 表：读配置先查表、表无回退默认），再做两块：
   - **设置页**（导航一级「设置」→ 每个设置项独立二级菜单；AI 设置内部 TAB 页区分子内容；**独立提交机制**——每个设置块独立表单/接口/局部刷新，settings 表按 key 原子更新）：①店铺管理（店铺表+管理页）②AI 设置（**v0.4 只做 API 密钥填写**，模型/风格 TAB 占位）③定时任务模块 ④通知配置（飞书）⑤系统参数（现有硬编码全量搬入，如 FOLLOW_UP_DAYS/每页条数/定时默认时间）。登录密码/角色 v0.4 不做（决策 36，随 v0.7 权限系统）
   - **定时闭环**：定时触发底座（schedule 表+调度器+去重）→ 首批定时链 CRM 客户超 N 天未跟进提醒（N 从 settings 表读）→ 任务自动进 TM
   - 权限系统留 v0.7；SEO/扒图/ERP 延后不排期。**启动前出详设 v0.4**（铁律 2 详设先行）
2. CRM / SEO / 扒图详设（对应版本启动前完成；SEO 已延后，随 v0.4 验证后再议）

## 样式打磨待办（2026-08-28 用户拍板：样式类问题不阻塞开发，集中后调）

- **拍板**：用户确认「样式相关问题后面再调整也可以」，v0.2 功能验收优先，样式打磨延后集中处理（不阻塞 tag v0.2 / v0.3 开发）。
- 已处理（v0.2 复核期）：筛选选择即生效、整体去彩色（徽章/按钮/提醒条白底+色边框+黑字）、outline 按钮与 btn-link 黑字、表格竖线统一、逾期行粗竖线清除。
- 待打磨（后续版本集中做，本次不做）：页面整体视觉精修（间距/对齐/字体层次/响应式断点）、逾期红「⚠ 逾期」与必填红星是否保留的最终确认、统计卡/侧栏等组件的现代感再评估。样式改动不影响 A12-A26 验收断言（均为功能断言），四绿不覆盖样式。

## 新会话接续指引（换会话/换人开发都按这个来）

1. 会话自动加载 CLAUDE.md（铁律 7 条），按铁律①先读本文件
2. 按本文「下一步」第 1 项开始干活（v0.4 调度完整版：先补 ⚑ 决策点 → 出详设 v0.4 → 再动代码）；开发任务严格按对应版本详设执行，逐任务走 `开发流程.md` 执行卡
3. 每个 commit 前跑 `scripts/check.sh` 四绿（全真跑；pre-commit hook 会自动跑，不绿不让提交）
4. 本会话的对话历史不是依据，**文档才是**--文档没写的决策不要凭记忆补，先补文档（变更日志留痕）

## 外部依赖状态

| 依赖 | 状态 |
|---|---|
| DeepSeek API | 可用（key 已有） |
| eHunt API | 可用（配额 200 条/天） |
| NocoBase PG | 生产运行（只读接入待 v0.6） |
| 阿里云服务器 | 已有（112.124.33.142，与 NocoBase 同机，部署待 v0.2 后） |
