# demo_echo_chain 链声明（详设-v0.1 §4.2；任务 T10）
# 链 id 与工序 id 不重名（loader L1 工序/链/provider 三表跨表全局唯一）。
# 步骤省略 input = 整链入参直传（§4.2 原文）。
id: demo_echo_chain
domain: demo
description: demo 演示链：整链入参直传 demo_echo 工序（结构化冒烟）
input:
  model: DemoEchoInput
steps:
  - worker: demo_echo
