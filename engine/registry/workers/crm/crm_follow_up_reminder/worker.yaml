# crm_follow_up_reminder 工序声明（v0.4 详设 §10.1；决策 37 纯代码提醒）
# 纯代码工序（reason: none，无 LLM 调用，零 token 成本）：
# 从 crm_overdue_context 拿超期客户清单 -> 逐客户生成提醒任务内容
# （title/detail/evidence 按代码模板生成，无 LLM 参与）。
id: crm_follow_up_reminder
domain: crm
version: 1
risk: suggest
description: CRM 未跟进提醒：逐超期客户生成提醒任务内容（纯代码，零 token）
input:
  model: ReminderChainInput
output:
  model: ReminderResult
model: default
reason: none
retry:
  max_attempts: 2
  timeout_s: 30
context:
  - id: crm_overdue_context
    params: {}
prompt: prompt.md
config_dir: config/
