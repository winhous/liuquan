# image_inspect 工序声明（详设-v0.5 §6.1）
# 纯代码工序（reason: none，无 LLM 调用，零 token 成本）：
# PIL 读宽高 + 水印启发式 → 输出 InspectionResult

id: image_inspect
domain: scrape
version: 2
risk: read
description: 扒图体检：PIL 读宽高 + 水印启发式判定（零 token）；批6 经 ctx.storage_dir 解析相对 local_path
input:
  model: ImageInspectInput
output:
  model: InspectionResult
model: default
reason: none
retry:
  max_attempts: 2
  timeout_s: 30
context: []
prompt: prompt.md
config_dir: config/
