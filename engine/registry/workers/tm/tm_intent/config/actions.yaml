# tm_intent 可解析动作清单（R10 规格外置；决策 28 流转自然语言入口）
# 动作名是技术枚举（词表来源）：run.py 从本 config 读取动作集合校验，不硬编码。
actions:
  transfer: 流转到某业务系统/域（如「流转到 ERP 库存」；目标域未接入时系统提示不可执行并记录意图）
  assign: 改派负责人角色（如「交给采购」）
  tag: 打/改标签（如「标成售后」）
  note: 记备注（如「备注等买家确认材质」）
  block: 挂起（如「挂起，等物料」）
