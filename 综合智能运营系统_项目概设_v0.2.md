# 综合智能运营系统项目概设
## v0.2

> 本文定位：项目总体架构设计（Conceptual Design）。
> 
> 本版本只确定系统的宏观目标、系统边界、分层、核心职责、集成关系和总体演进方向。
> 不进入数据库结构、API 参数、Skill 加载算法、具体 Agent Prompt、字段级权限等详细设计。
> 所有尚未确定的工程问题统一放入文末“待办与详设问题”。

---

# 1. 项目背景

当前业务已经存在多个相对独立的系统和能力：

- 自研 ERP
- 现有 CRM / 任务管理能力（EMOS）
- 现有 Studio AI 平台
- SEO 数据与分析能力（计划接入 EHUNT）
- Etsy 及未来其他电商平台业务

现有系统能够分别解决部分问题，但各系统之间尚未形成统一的业务闭环。

本项目的目标不是简单重新开发一个 ERP、CRM 或 AI Agent，而是建设一个：

> **以业务系统为基础、以 AI 为智能决策与任务生成能力、以任务管理为人机协作出口的综合智能运营系统。**

系统最终形成：

```text
业务系统产生事实
        ↓
AI理解业务状态
        ↓
AI判断应该做什么
        ↓
形成任务/建议
        ↓
TM分配给人
        ↓
人执行
        ↓
反馈结果
        ↓
AI判断下一步
        ↓
形成新的任务或结束
```

---

# 2. 总体设计原则

## 2.1 业务系统与 AI 解耦

ERP、CRM、SEO、TM 不应直接依赖某个具体大模型，也不应把 AI 逻辑散落在各业务系统内部。

业务系统负责：

- 业务数据
- 业务状态
- 业务规则
- 业务事务
- 权限
- 最终数据写入

AI 平台负责：

- 理解
- 分析
- 推理
- 规划
- 内容生成
- 任务建议
- 多步骤 AI 工序执行

二者通过统一的 AI 集成层连接。

## 2.2 AI 不取代业务系统

AI 不应该成为 ERP、CRM、SEO 的数据库访问层，也不应该成为最终业务规则的拥有者。

例如：

> AI 可以判断“建议采购 100 个”。

但库存数量、销量、供应商、采购单、最终采购状态等事实和事务仍由 ERP 管理。

AI 是“智能决策层”，不是“业务事实层”。

## 2.3 Studio 与 Studio Engine 分离

现有 Studio 并非单纯模型调用器。Studio 已经包含 AI Engine、Skill、Agent、Workflow / 工序、Tool、Policy、Context、Memory / Knowledge、Registry、执行记录等能力。

因此新系统不会简单地把 Studio “去业务化”。

总体原则是：

> **Studio 保留作为 AI 业务能力平台；Studio Engine 保持业务无关。**

Studio 中可以存在业务 Skill，例如 CRM 分析、SEO 分析、Listing 优化、客户对话分析、任务结果分析等。

但是 Engine 本身不应该知道 Etsy、SKU、客户、采购等具体业务概念。

---

# 3. 总体架构

系统总体采用四层结构：

```text
┌───────────────────────────────────────────────┐
│                 业务应用层                    │
│                                               │
│       ERP       CRM       SEO       TM        │
└──────────────────────┬────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────┐
│          Business AI Integration Layer        │
│                                               │
│   业务事件 / Context / Query / Action /       │
│   Task / Result / Permission / Contract       │
└──────────────────────┬────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────┐
│                     Studio                    │
│                                               │
│   Business Skills / Agents / AI Workflows     │
│                     │                         │
│              ┌──────▼──────┐                  │
│              │ Studio Engine│                  │
│              └──────┬──────┘                  │
└─────────────────────┼─────────────────────────┘
                      │
                      ▼
              LLM / AI Models
```

四层职责明确：

### 业务应用层

管理真实业务。

### Business AI Integration Layer

连接业务系统与 AI，定义双方共同遵守的标准。

### Studio

提供 AI 业务能力和 AI 工作流。

### Studio Engine

负责 AI 能力的运行、编排、控制和执行。

---

# 4. ERP

ERP 是综合系统的业务基础之一。

主要负责：

- 商品 / SKU
- 库存
- 采购
- 供应商
- 订单
- 发货
- 物流
- 平台
- 店铺
- 财务及经营分析
- 多平台、多店铺运营基础数据

ERP 的核心原则：

> **ERP 是业务事实和业务事务的权威来源。**

AI 可以读取 ERP 提供的业务上下文，可以提出采购、补货、库存处理等建议，也可以产生任务。

但 AI 不直接拥有 ERP 的最终业务控制权。

---

# 5. CRM

CRM 主要负责客户关系与客户沟通相关业务。

初期包括：

- 客户资料
- 历史对话
- 手动导入对话
- 对话翻译
- 对话分析
- 客户需求识别
- 跟进事项
- 报价事项
- 客户相关任务

CRM 不需要承担完整 AI Runtime，AI 能力通过 Studio 提供。

---

# 6. SEO

SEO 是独立业务模块。

主要包括：

- EHUNT 数据接入
- 关键词研究
- 市场研究
- 老商品 SEO 优化
- 新商品关键词生成
- Listing 分析
- SEO 建议
- SEO 相关任务

SEO 自己负责 SEO 数据、SEO 状态以及 SEO 业务结果；Studio 负责对这些数据进行分析、推理、生成和规划。

---

# 7. TM：任务管理中心

TM 是整个系统的人机交互核心。

系统最终不是要求 AI 自动完成所有现实世界的事情，而是：

> **AI 发现事情 → AI形成任务 → 人执行 → 人反馈 → AI决定下一步。**

任务来源包括人工创建、AI 创建、任务完成后产生的后续任务。

---

# 8. 任务闭环

TM 不仅是一个 Todo List。核心模型是：

```text
任务
 ↓
执行
 ↓
结果
 ↓
判断
 ↓
下一步
```

任务需要能够承载目标、负责人、截止时间、状态、执行结果、后续判断和后续任务。具体数据结构在详设阶段确定。

---

# 9. Business AI Integration Layer

这是新系统的重要新增架构层。

它的核心目的不是简单做 API Gateway，而是建立：

> **业务系统与 AI 系统之间统一的业务交互规范。**

宏观上包括：

```text
Business Event
Business Context
Business Query
Business Action
Task Proposal
Task Result
Permission / Policy
Business AI Contract
```

AI 不直接访问 ERP、CRM、SEO 的内部数据库，而是通过 Integration Layer 获得标准化业务上下文与受控能力。

---

# 10. Business AI Contract

ERP、CRM、SEO、TM 等模块应遵守统一的 AI 接入规范。宏观上统一以下概念：

```text
Entity
Event
Context
Query
Action
Task
Task Result
Permission
Result
```

这样未来新增 Amazon、Shopify、广告、财务、供应链、Marketing 等模块时，不需要重新设计一套 AI 接口体系。

---

# 11. Studio 的定位

Studio 在新系统中不是业务数据库，也不是 ERP/CRM/SEO 的替代品。

Studio 的定位是：

> **AI 业务能力平台。**

它负责 AI Skill、Agent、AI Workflow、Context 使用、Tool 调用、AI 推理、内容生成、多步骤 AI 工序、AI 状态管理、Policy 和 AI 执行记录。

Studio 可以拥有业务 Skill，但这些 Skill 不应绕过 Integration Layer 直接访问业务系统。

---

# 12. Studio Engine 的定位

Studio Engine 是 Studio 的底层执行能力。它不理解具体业务。

主要负责：

- Runtime
- Skill 执行
- Agent 执行
- Workflow
- State
- Policy
- Tool
- Model
- Session
- Checkpoint
- Artifact
- Event
- Log

原则：

> **Engine 可以执行业务 Skill，但 Engine 本身不拥有业务。**

---

# 13. AI 与代码的边界

整个系统采用 **Code + AI**，而不是 AI Everything。

代码负责确定性问题：数据一致性、状态机、权限、业务事务、金额计算、库存扣减、数据写入、必须满足的规则和最终状态转换。

AI 负责非确定性问题：对话理解、信息提取、分类、总结、推理、建议、规划、SEO 分析、市场分析、下一步判断、自然语言转任务。

核心原则：

> **AI 可以提出，代码负责验证；AI 可以判断，业务系统负责最终执行。**

---

# 14. 事件驱动

系统总体采用事件驱动思想。

例如：

```text
库存低于阈值
        ↓
InventoryLow Event
        ↓
AI分析
        ↓
Task Proposal
        ↓
TM任务
```

CRM、SEO 等业务模块同样通过业务事件触发 AI 分析和任务产生。

---

# 15. 人机协作模型

核心模式是：

```text
AI发现
 ↓
AI判断
 ↓
AI计划
 ↓
人执行
 ↓
人反馈
 ↓
AI继续判断
```

系统的目标不是 AI 替人工作，而是：

> **AI 持续告诉团队下一件最值得做的事情，并让事情形成闭环。**

---

# 16. 数据与权限原则

业务数据仍然归属于业务系统。Studio 不应成为企业数据的最终权威来源。

权限也不应该依靠 Prompt 实现，而应由业务系统、Integration Layer 和 Policy 共同控制。

初期优先采用：

> AI 建议 → 人执行

而不是 AI 自动修改核心业务数据。

---

# 17. 多平台、多店铺设计

ERP 从一开始应考虑平台、店铺、商品/SKU、订单/库存/物流等统一业务模型。

平台差异通过适配层处理，不让 Etsy 的特殊实现污染整个业务模型。未来可以扩展 Etsy、Amazon、Shopify 等平台。

AI 看到的是统一业务 Context，而不是各个平台的原始数据结构。

---

# 18. 现有系统的总体演进

现有系统不要求一次性全部推翻。总体采用：

> **保留已有成熟能力 → 清理问题 → 按统一架构逐步整合。**

ERP 作为业务基础进行重构/迁移；EMOS 中已验证的 CRM/TM 能力按综合系统架构整合；Studio 保留 Engine、Skill、Agent、Workflow、Policy 等 AI 平台能力，同时逐步解决其与具体业务绑定、Skill 组织方式等问题。

---

# 19. 新系统总体闭环

```text
业务事实
   ↓
业务事件
   ↓
AI Context
   ↓
Studio Skill / Agent
   ↓
AI判断
   ↓
Task Proposal
   ↓
TM
   ↓
人执行
   ↓
Task Result
   ↓
业务系统
   ↓
下一轮AI判断
```

这是整个系统的核心业务循环。

---

# 20. 架构扩展原则

未来增加业务模块时：

```text
新业务系统
    ↓
统一 Business AI Contract
    ↓
Business AI Integration Layer
    ↓
Studio
```

不应让新业务系统直接绑定某个模型，也不应让新业务系统直接修改 Studio Engine。

---

# 21. 当前明确的架构决策

1. **Studio 保留，不推翻。**
2. **Studio Engine 与业务解耦。**
3. **Studio 可以承载业务 Skill。**
4. **ERP、CRM、SEO、TM 作为独立业务模块。**
5. **增加 Business AI Integration Layer。**
6. **业务系统不直接依赖具体 LLM。**
7. **AI 不直接成为业务数据库和事务系统。**
8. **TM 是人机协作的核心出口。**
9. **采用 Code + AI 双轨架构。**
10. **业务事件驱动 AI 分析和任务产生。**
11. **AI 主要负责理解、分析、判断、规划；代码负责确定性业务控制。**
12. **未来新增业务模块遵守统一 AI Contract。**

---

# 22. 待办与详设问题

以下问题不进入总体架构结论，统一留到后续详细设计。

## 22.1 Studio Skill 加载机制

当前 Studio 存在启动/加载阶段可能加载大量 Skill 的问题。需要研究：按项目加载、按业务域加载、Skill Package、Registry 拆分、动态发现、懒加载、生命周期管理等。

**本阶段不做实现结论。**

## 22.2 Studio Skill 的归属

逐个审查现有 Skill，判断哪些属于 Studio 通用能力、ERP、CRM、SEO、内容生产或独立插件。

**本阶段只确定“需要分类”，不进行具体迁移。**

## 22.3 Business AI Integration Layer 的具体实现

详设阶段确定服务边界、API/Event 总线、Context/Action/Result 标准、权限传递、认证、错误处理、版本兼容、日志与审计。

## 22.4 Business AI Contract

形成正式的 Entity、Event、Context、Query、Action、Task、Task Result、Permission、Result 规范及版本管理方式。

## 22.5 Studio 与 Integration Layer 的关系

明确 Skill 是否只能通过 Integration Layer 访问业务、Studio Tool 如何提供、哪些通用 Tool 保留在 Studio、哪些业务 Tool 由业务模块提供。

## 22.6 AI 自动执行边界

建立从只读分析、生成建议、创建任务、修改业务数据到执行业务事务的风险等级与审批策略。

## 22.7 TM 详细模型

详设 Task、Task Result、Follow-up、Dependency、Deadline、Assignment、AI Proposal、Human Created Task、Recurring Task、Event-triggered Task 等模型。

## 22.8 Context 机制

确定 Context 的按需获取、缓存、版本、生命周期及无关数据隔离机制。

## 22.9 Agent / Skill / Workflow 边界

明确三者的职责与组合方式。

## 22.10 现有系统迁移策略

分别制定 ERP、EMOS CRM/TM、Studio 的迁移、重构与兼容策略。

---

# 23. 下一阶段

建议按以下顺序进入详细设计：

```text
Business AI Contract
        ↓
Business AI Integration Layer
        ↓
Studio 与 Integration Layer 集成
        ↓
TM 详细设计
        ↓
ERP / CRM / SEO 详细设计
        ↓
AI Skill / Agent / Workflow 详细设计
        ↓
迁移与实施计划
```

其中第一阶段最重要：

> **Business AI Contract 一旦稳定，ERP、CRM、SEO、TM 和 Studio 就有了共同语言。**

---

# 24. 概设结论

本项目不是传统 ERP + CRM + SEO + Task Manager 的简单拼接，也不是把所有业务交给一个 AI Agent。

它是一套由：

> **业务系统 + AI 业务能力平台 + AI 集成层 + 人类任务执行闭环**

组成的综合智能运营系统。

其中：

```text
ERP / CRM / SEO
    = 业务世界

TM
    = 人类执行世界

Business AI Integration Layer
    = 业务世界与 AI 世界之间的统一接口

Studio Business Skills
    = AI 的业务能力

Studio Engine
    = AI 能力的运行基础

LLM
    = AI 的推理能力
```

最终形成：

> **业务产生事实，AI理解事实并判断下一步，TM让人执行，执行结果再次进入业务系统和 AI，形成持续循环。**

这是本项目的总体架构基线。

---

**版本：v0.2**  
**状态：概设阶段**  
**下一阶段：Business AI Contract + Integration Layer 详设**
